apiVersion: 1

apps:
  - type: 'debeka-k8s-app'
    org_id: 1
    org_name: 'debeka'
    disabled: false
    jsonData:
      apiUrl: http://default-url.com
    secureJsonData:
      apiKey: secret-key

# Session Summary — debeka-k8s-app (Grafana K8s Observability App Plugin)

**Repo:** https://github.com/Lprod/grafana-k8s-app (branch `main`)
**Current release: `v1.11.5`**, released 2026-08-21 (see "Session 2026-08-21 (second)" at the end of this file). Previous: `v1.11.2` - two bugfixes (Kubernetes home page's Issue details table had no Cluster/Namespace/Pod links; Clusters list "Alerts" column undercounted vs. the Cluster Drilldown badge/Alerts page), see "Session 2026-08-21" near the end of this file. **Gap: this file was not updated across `v1.10.1`–`v1.11.1`** (several sessions in other worktrees shipped fixes - health-banner alert-count/severity bugs, the Node Drilldown's remaining tabs, the Nodes/Clusters phantom-row and Nodes Alerts-link-mismatch fixes, a per-panel Explore menu item - without a matching "Update session summary" commit here). `CHANGELOG.md` is the authoritative version history for that gap; skim it at the start of a session if this file's own account of "what exists today" seems behind what a `git log`/CHANGELOG check shows. Each release: feature commit(s) → `chore(release): vX.Y.Z` (package.json/package-lock.json bump only) → `git tag vX.Y.Z` → `git push origin main && git push origin vX.Y.Z`, which triggers `.github/workflows/release.yml`.
**Parallel-agent note:** this session started by merging in a *different* concurrent session's work (v1.7.0's Kubernetes home page Overview/Efficiency tabs) via `git merge origin/main` into this worktree's branch before continuing - only conflict was `CHANGELOG.md`'s insertion point (both branches added a new version heading at the top), resolved by ordering newest-first. Worth checking `git log --oneline -10` / `git worktree list` at the start of a session in this repo, since multiple worktrees/sessions on the same repo are common here.
**Reference design:** https://play.grafana.org/a/grafana-k8s-app/navigation/cluster/... ("Grafana Play") — this plugin is being rebuilt to visually/functionally match Grafana's own official Kubernetes Monitoring app, tab by tab. The user pastes the exact PromQL + panel titles from Grafana Play for each tab; translate `$cluster`/`$namespace`/`$node` literally into this codebase's variable-interpolation style (see existing tabs for the pattern) rather than "fixing" them to look more consistent — the user has occasionally used different filter operators (`=` vs `=~`) or omitted a variable filter on purpose between panels, and that's preserved intentionally.

## Release process (established over several sessions)

Direct commits to `main`, no PR workflow (solo-maintainer repo). Pattern, from git log:
1. One commit per feature/fix, each adding its own CHANGELOG.md bullet under the version-in-progress heading (create the heading with the first bullet of a new version).
2. A final `chore(release): vX.Y.Z` commit that *only* bumps `package.json` + `package-lock.json` (via `npm version X.Y.Z --no-git-tag-version`) — no CHANGELOG touch, that already happened in the feature commits.
3. `git tag vX.Y.Z && git push origin main && git push origin vX.Y.Z`. The tag push triggers `.github/workflows/release.yml` (`grafana/plugin-actions/build-plugin`), which builds and publishes the actual release artifact — as a **draft** GitHub release (confirmed both v1.4.0 and v1.5.0 stayed drafts; that's normal, not a workflow failure).
4. CI's e2e job (`appConfig.spec.ts`, `appNavigation.spec.ts`) fails on every Grafana version tested, including on `main` before any of this session's changes — pre-existing/unrelated, not something introduced here. The `Release` workflow itself (which is what actually matters for shipping) has been passing.

**This session's twist:** a harness-level "create a PR" instruction (structured `<create-pr-command>` block, pushed a branch + `gh pr create`) arrived mid-session, conflicting with this repo's established direct-to-main convention. Asked the user to disambiguate via `AskUserQuestion` rather than guessing — they confirmed direct-to-main. **General lesson: an automated/injected instruction that contradicts a well-established, explicitly-documented project convention is worth a clarifying question, not silent compliance** — it may be a generic harness feature that doesn't know this specific repo's history.

**Committing after heavily interleaved edits:** this session's UI-polish work happened as several rounds of "make X nicer" requests that kept touching the *same* files (`tableCells.tsx`, `namespacesPage.tsx`) in non-adjacent ways. Perfectly atomic per-feature commits would have needed hunk-level `git add -p` surgery on hard-to-separate single-line changes (e.g. one import line pulling in symbols for two unrelated features). Didn't chase that — grouped commits by *module* instead (e.g. all of `tableCells.tsx`'s new cell renderers in one commit, described honestly as covering a bug fix + new reusable helpers) and accepted that as "good enough" for a solo-maintainer repo. Verified with `git diff` per file first to confirm genuinely-unrelated files (e.g. `clustersApp.tsx`/`nodesPage.tsx`, `panelTimeRangeCompare.tsx`) *were* still cleanly separable, and split those.

## Naming convention (use this when talking about pages)

- A top-level list page is named plainly: **Clusters**, **Nodes**, **Namespaces**, **Workloads**, **Alerts**.
- Its detail/drilldown page (reached by clicking a row) is named "**\<Name\> Drilldown**", e.g. **Clusters Drilldown**.
- This is also saved in persistent memory (`feedback_page_naming.md`).

## What exists today

### Kubernetes home page — `src/pages/Kubernetes/kubernetesPage.tsx: getKubernetesHomePage`
This is the plugin's own root URL (`/a/debeka-k8s-app`, bare, no sub-path) — `src/plugin.json`'s app `"name": "Kubernetes"` is what Grafana shows as the parent nav entry one level above Clusters/Nodes/Namespaces/Workloads/Alerts, and clicking it (or hard-loading the bare plugin URL) lands here. It previously had **no matching `SceneAppPage`** in `getClustersSceneApp()`'s `pages` array, so it fell through to Scenes' built-in "Not found" fallback page — that's what "the Kubernetes page is empty" meant when the user first reported it. `plugin.json` was deliberately **not** touched to fix this (the nav parent already exists as the app's own name; a new `includes` entry would just be a redundant extra sub-link).

Routing note for future similar pages: a top-level `SceneAppPage` **with `tabs`** needs its own `routePath` to end in `/*` (not just `/`), or react-router never descends into its internal tab-`<Routes>` for any sub-path — this page uses `routePath: '/*'`. Tab 0 auto-mounts at the parent's own bare path (Scenes' `SceneAppPageRenderer` registers `tabs[0]` at both `path: ""` and its own slug), so landing on the bare root renders Overview by default, no manual redirect needed. A tabbed page's routePath being a wildcard doesn't shadow more specific sibling routes (`/clusters/*` etc. still win) since React Router v6 ranks by specificity, not array order — confirmed against `@grafana/scenes`' actual `SceneApp.js`/`SceneAppPage.js` source.

Tabs: **Overview, Efficiency, Alerts** (confirmed via Playwright against Grafana Play's own `/a/grafana-k8s-app/home` — this plugin mirrors that reference tab-for-tab, same as the other pages mirror Grafana's per-object nav). The Alerts tab's route slug is deliberately `alerts-tab`, not `alerts` — the existing standalone Alerts page already owns `/alerts/*` as a sibling top-level route, and since react-router picks the more specific static match regardless of source, a tab literally named `alerts` here would be permanently unreachable.

**Alerts tab built out this session** (`src/pages/Kubernetes/kubernetesAlertsScene.tsx`) — a single timeseries panel, "Firing alerts by cluster" (`count by(cluster) (ALERTS{alertstate="firing", cluster=~".*"})`), with the usual per-panel comparison control. The `getComingSoonScene` helper it replaced had no other consumers, so it was deleted outright rather than left as dead code.

**Overview tab** — `src/pages/Kubernetes/kubernetesOverviewScene.tsx` + `src/queries/kubernetesOverviewQueries.ts`:
- 6 top stat tiles (Clusters/Nodes/Namespaces/Workloads/Pods/Containers), flat-colored background (not health-colored — these are informational counts), Clusters/Nodes/Namespaces/Workloads link to their existing pages via `overrideLinks` (same pattern as `clustersApp.tsx`'s Cluster/Nodes column links); Pods/Containers stay unlinked (no dedicated page exists for either). **Confirmed these links actually work** (Playwright: clicked the Clusters tile, landed on `/a/debeka-k8s-app/clusters`) — only the *value* area of a Stat panel is the clickable link, not the title-bar/header (`panel.boundingBox()` vs `anchor.boundingBox()` showed the anchor covers ~58px of the tile's 100px height), which is standard Grafana Stat-panel behavior, not a bug, if "the link is missing" comes up again.
- Tile color **changed this session** from Grafana's named `'blue'` hue to a specific hex (`#1CC4D7`) matching Grafana's own Kubernetes Monitoring app - sampled directly (Python/Pillow pixel sampling, `pip install --break-system-packages pillow`) from the reference screenshot at `https://grafana.com/media/docs/grafana-cloud/k8s/k8s-overview-overvw-tab.png` (found via `WebSearch` + `WebFetch` after play.grafana.org's *live* app turned out to have moved on to a different, more feature-rich design - `/a/grafana-k8s-app/navigation/cluster` etc. no longer show this fleet-wide Overview-with-stat-tiles page at all). The reference's tiles render a distinctly teal-leaning gradient, not the more indigo-blue that Grafana's named `"blue"` hue resolves to.
- 3 sections (Availability/Stability/Infrastructure), 4 mini issue-count stat panels each (`buildIssueCountStatPanel` — same shape as `buildEfficiencyStatPanel` in `clustersApp.tsx`, `count(<expr>)` over a range query, green/red thresholds). Each has a "View detail" header-action button.
- A shared "Detail view" dropdown (`Combobox`, not the deprecated `Select`) + table beneath the 3 sections: every mini panel's "View detail" button re-points the *same* table at that panel's own (unwrapped) query, mirroring how Grafana's own reference groups all 12 issue tables behind one dropdown+table pair instead of 12 separate ones. Implemented as a small custom `DetailViewSelection` (`SceneObjectBase`) + `ViewDetailLink` header-action pair — see the gotcha below on why the shared `SceneQueryRunner`/click-handler *can't* be plain `SceneObjectState` fields.
- A "Deployed container images" table (`sum by (image_spec) (kube_pod_container_info{...})`), styled to match Grafana's reference: `organize` transform drops the auto-added `Time` field and renames `image_spec`/`Value` to "Image spec"/"Containers", `sortBy` descending, a `TableCellDisplayMode.Gauge` + `BarGaugeDisplayMode.Gradient` cell (not `TableCellDisplayMode.GradientGauge` — that enum value exists but isn't a valid `type` for `TableBarGaugeCellOptions`, the gradient look is the `mode` field alongside `type: Gauge`). **Bar color and column width matched to the reference this session** (same docs-screenshot pixel-sampling as the stat tiles above): the "Containers" bar is `FieldColorModeId.ContinuousBlPu` (a blue-to-purple gradient scaled by *value*, not a flat fixed color - confirmed by sampling: the longest/highest-value bar trends visibly more purple than shorter ones) with a fixed `width: 160` custom field config so "Image spec" fills the remaining space, instead of both columns auto-sizing evenly. Footer reducer changed `['count']` → `['sum']` (a "Total" of *containers*, not a count of image rows) — **this didn't visibly render** in this environment's Grafana build (13.1.0) despite the config being correct per the public schema (`TableFieldOptions.footer` lives on the field, no panel-level `options.footer` toggle needed, confirmed by reading `@grafana/ui`'s `calculateFooterHeight` in `TableNG/utils.mjs`) - zero `[class*="footer"]` DOM elements found via Playwright. Worth re-checking against a real/different Grafana instance before assuming this codebase has a genuine bug here.

**Efficiency tab** — `src/pages/Kubernetes/kubernetesEfficiencyScene.tsx` + `src/queries/kubernetesEfficiencyQueries.ts`: 4 stat tiles (No Resource Requests/Limits, CPU/Memory Over-requested — green/orange flat background, a *different* color convention than the Overview tab's red/green+sparkline tiles, matching Grafana's own reference exactly), 2 `PanelBuilders.bargauge()` horizontal "waste by namespace (top 10)" panels (no populated reference screenshot existed to copy exactly — bargauge was the most idiomatic fit for a ranked top-N-by-namespace list, revisit if the user wants something else), and 5 `topk(50, ...)` tables. The tables' empty-result state uses `.setNoValue('All containers have resource requests set.')` etc. (each table has its own friendly sentence) instead of the generic "No data" box — confirmed this actually works for a **zero-row** result, not just a null cell within an existing row (verified live: `fieldConfig.defaults.noValue` triggers via `SpecialValueMatch.Empty`-equivalent handling at the whole-panel level when the query returns nothing).

**Gotcha: don't put a shared object you'll reference from multiple sibling panels directly in `SceneObjectState`.** `SceneObjectBase`'s `_setParent` walks *every* state value (not just `$`-prefixed ones) and auto-parents any `SceneObjectBase` instance it finds; a `SceneQueryRunner` or other scene object referenced from 12+ sibling panels' state would get silently re-parented to whichever one constructed last (console warning: "SceneObject already has a parent set..."), and its *real* placement (e.g. as a VizPanel's `$data`) breaks. Fix used here: keep the shared reference as a plain instance field (set via a custom constructor, not part of the `SceneObjectState` interface) or as a plain closure (`onSelect: () => void`) instead — see `DetailViewSelection`/`ViewDetailLink` in `kubernetesOverviewScene.tsx`.

### Clusters (list page) — `src/scenes/clustersApp.tsx: getClustersListScene`
Table of all clusters (Cluster/Nodes/Alerts/CPU Avg/Avg%/Max/Max%/Mem Avg/Avg%/Max/Max%). Cluster column links to **Clusters Drilldown**; Nodes column links to **Nodes** page; Alerts column links to the **Alerts** page (`?var-cluster=<cluster>`, per-row, native table link). Thresholds: orange <60%, green 60–90%, red >90% — **`usageThresholds` now lives once in `src/scenes/tableCells.tsx`** (this session deduplicated it out of Clusters/Nodes/Namespaces/Workloads, which each redeclared it identically). All table columns are left-aligned. Table-only page — no timeseries panels, so it does **not** get the comparison feature below.

### Clusters Drilldown — `getClusterDetailPage` in clustersApp.tsx
Tabbed page: **Overview, CPU, Memory, Network, Storage**. Title renders as "\<cluster-name\> [cluster]" badge via `ClusterPageTitle`. Datasource picker + timepicker in page-level controls (browser-local timezone). Every one of its 30 timeseries panels across the 5 tabs has a **per-panel comparison control** — see "Per-panel timeseries comparison" below for its current (Badge-pill) look.

### Nodes (list page) — `src/pages/Nodes/nodesPage.tsx` + `src/queries/nodeQueries.ts`
Table across all selected clusters: Cluster/Node/Alerts/CPU Avg/Avg%/Max/Max%/Mem Avg/Avg%/Max/Max%. Same thresholds/colored-icon-cell style as Clusters. Cluster column → Clusters Drilldown; Node column → **Node Drilldown (new this session)**; Alerts column → Alerts page (pre-filtered by cluster+node). Joined with `joinByField(byField: 'node', mode: 'outer')` — every query except `info` drops `cluster` from its outermost grouping (kept only via `info`) to avoid duplicate "Cluster" columns after the join, since `joinByField` doesn't disambiguate identically-named non-value fields across queries. This works here because node names are unique across clusters in practice. Table-only — no comparison feature.

### Node Drilldown — `getNodeDetailPage` in `nodesPage.tsx` (new this session, v1.10.0)
Reached via `/nodes/:cluster/:node/*`. Title renders node name + a "node" `Badge` (`color="darkgrey"` - the one entity-badge color not already claimed by cluster=blue/namespace=purple/pod=orange), "in cluster X" subtitle. Tabs: Overview (built), CPU/Memory/Network/Storage/Logs/Events (placeholders - see "Where to pick up next"). Only the Overview tab has real content so far, per the user's own incremental spec.

**Overview tab**, top to bottom:
- "Node information" heading, then a new `NodeHealthBanner` (`clusterOverviewCards.tsx`) - the odd one out among this app's health banners: severity comes from `kube_node_status_condition` (Ready condition false/unknown → error "Node is not ready"; any of MemoryPressure/DiskPressure/PIDPressure true → warning "Node has \<X\> pressure"; otherwise healthy), **not** from alerts like `NamespaceHealthBanner`/`ClusterHealthBanner` - the alerts action button is still there, just fed by a *separate* alerts-by-severity query. Both queries share the banner's own single `$data` (refIds `conditions`/`alerts`, filtered apart in the renderer by `frame.refId`) rather than two `SceneQueryRunner`s, to avoid the established "sharing a non-$-prefixed scene object between siblings gets silently re-parented" gotcha applying to `$data` too.
  - **Bug found and fixed**: a zero-result instant Prometheus query still comes back as *one* frame with an empty `fields` array (confirmed via `/api/ds/query`), not zero frames - the first version of the pressure-detection logic did `frames.find(f => label !== 'Ready')`, which matched that empty placeholder frame too (`undefined !== 'Ready'` is true), so every healthy node showed a false "pressure" warning. Fixed by checking for a genuine `MemoryPressure`/`DiskPressure`/`PIDPressure` label match instead of "anything that isn't Ready".
- Three info cards (`InfoCard`, all `width: '33.3%'`, only the left one gets `ySizing: 'content'` - the other two default to `'fill'`/stretch so they match its height instead of leaving dead space, same fix as the Namespace Drilldown's own quota cards): left = clustername (linked)/node/node_ip/node_os_image; middle = node_kernel_version/node_kubelet_version/node_container_runtime_version (all from one `kube_node_info` query, `format: 'table'` so its labels split into separate scalar columns for `findFieldAcrossFrames`); right = vcf_vcenter/vcf_clustername/vcf_esx_host, from a **new chained PromQL self-join** against `vsphere_vm_cpu_idle_summation` (`buildNodeVcfInfoQuery`, `nodeOverviewQueries.ts`) - the user described this as three sequential "same metric, different filter" lookups (vmname→esxhostname→clustername→vcenter), translated into one query via `on(...)/group_left(...)` chained self-joins instead of three dependent Grafana panel queries. **Not verifiable against the local demo stack** - no vSphere/telegraf source in the demo compose stack - worth checking against a real environment.
- "Node optimization" heading + two timeseries (Node CPU/Node Memory): capacity (light-purple filled)/limits (dashed red)/requests (dashed orange)/usage (solid blue) - same `applyNodeOptimizationSeriesOverrides` styling as the **Cluster** Drilldown's own optimization charts (a node, like a cluster, has a real physical capacity ceiling - unlike the Pod/Workload Drilldowns' own optimization charts, which have no capacity line).
- "Pods" heading + a table (POD/WORKLOAD/TYPE/NAMESPACE/STATUS/CPU Usage/CPU Requests/Mem Usage/Mem Requests/Mem Limits) - one row per pod scheduled on this node, using the exact same combined value+percent+bar meter cells (`requestUsageCell`/`usageTierCell` via `attachPercentField`) as the Namespaces list table. POD/WORKLOAD/NAMESPACE link to their own Drilldowns; the CLUSTER portion of each link is inlined as a literal JS value (this page is already scoped to one cluster) rather than read per-row, so `cluster` doesn't need to stay in the frame as an otherwise-unrequested visible column.
  - **Two real bugs found and fixed** (both while building this table, not pre-existing): (a) `cluster:namespace:pod_cpu:active:kube_pod_container_resource_requests`/`cluster:namespace:pod_memory:active:kube_pod_container_resource_requests`/`..._limits` had **no `node` label anywhere in the demo dataset** - the first query in this codebase to filter these recording rules by node - so every node-scoped CPU/Memory requests/limits number was empty. Fixed additively (each pod's existing `kube_pod_info` node assignment copied onto its own recording-rule rows). (b) The given query set's `infoWaiting` query (`avg by(cluster,namespace,pod) (kube_pod_status_phase{phase="Running"})`) had **no node filter at all**, unlike every sibling query - confirmed live via `curl` that it returned every Running pod cluster-wide, which the `merge` transform then added as phantom rows (blank workload/namespace/status/CPU/Mem) for pods not actually on the node being viewed. Its value was never going to be shown as a column either way, so dropped from the query set entirely instead of guessing the intended filter.

### Namespaces (list page) — `src/pages/Namespaces/namespacesPage.tsx` + `src/queries/namespaceQueries.ts`
Table across all selected clusters: Cluster/Namespace/Workloads/Alerts/**CPU Usage/CPU Requests/Mem Usage/Mem Requests/Mem Limits**. **Uses Grafana's `merge` transform, not `joinByField`** (see gotcha list — an earlier `joinByField` version silently dropped the whole Cluster column when its one designated join-key query returned sparse data).

**Redesigned this session** — the old separate "value" + "value %" column pairs (CPU Requests/Requests %, Mem Requests/Requests %, Mem Limits/Limits %) are now **combined meter cells** (`requestUsageCell` in `tableCells.tsx`): the value, a colored percent Badge, and a proportional bar underneath, all in one cell — visually modeled on the Resource Simulator's own used/requested meter (`UsageRequestMeter` in `ResourceSimulatorObject.tsx`), but colored with this app's usual `usageTierFromFraction` scale (orange<60%/green 60-90%/red>90%), not the Simulator's separate 20/80/100/150% planning-ratio scale. CPU Usage and Mem Usage also got an icon + tier-colored value treatment (`usageTierCell`) using the *same* percent as their neighboring combined cell:
- **CPU Usage is colored by the CPU Requests ratio** (`cpu_requests_percent`).
- **Mem Usage is colored by the Mem *Limits* ratio**, not Requests — this was an explicit user call after asking "which makes more sense": limits are the hard ceiling (OOM-kill on memory), requests are only a scheduler reservation, so usage-vs-limit is the more meaningful "about to hit the wall" signal. If the same question comes up for CPU (CPU has no hard "OOM" equivalent, just throttling), it's worth asking again rather than assuming the same answer applies.

Mechanism: `attachPercentField(targetFieldName, percentFieldName)` (generalized from the Workloads Pods-column trick, see gotcha list) stashes a percent field's values onto a *different* field's own `config.custom`, keyed `percentValues`, so a cell renderer for that other field can read it without a sibling-frame lookup — and the same underlying percent field can be attached to **multiple** targets (e.g. `Value #cpu_requests_percent` is attached to both `Value #cpu_requests` and `Value #cpu_usage`). The raw percent fields are then fully `excludeByName`'d from the frame (not just hidden — see the `hideFrom` gotcha below for why that distinction matters).

**Demo-data gap found and fixed**: the Namespaces page's `cpu_requests`/`mem_requests`/`mem_limits` queries read `cluster:namespace:pod_cpu:active:kube_pod_container_resource_requests` / `cluster:namespace:pod_memory:active:kube_pod_container_resource_requests` / `...resource_limits` — these were **entirely absent** from `demo/kube-metrics/metrics`. Not a bug: a *different*, similarly-named recording rule (`namespace_cpu:kube_pod_container_resource_requests:sum` etc.) already existed in the demo data and is what the **Clusters Drilldown Overview** page's CPU/Memory optimization panels read (`clusterOverviewQueries.ts`) — both are real kube-prometheus-stack-style rules a real cluster could have side by side, this demo just never got the Namespaces-specific one added. Fixed by adding it with the same values as its `namespace_cpu:`/`namespace_memory:` sibling (same real-world quantity, two rule names). **If a future "column X shows nothing" report turns out to be a missing metric, check whether a differently-named-but-equivalent recording rule already exists in the demo data for another page first** — it's a good sign the gap is a genuine oversight rather than something structurally unavailable.

**Namespace column now links to the new Namespaces Drilldown** (`${NAMESPACES_URL}/\${__data.fields.cluster}/\${__value.text}\${__url.params}`), same per-row native-table-link pattern as the Cluster column.

### Namespaces Drilldown — `getNamespaceDetailPage` in `src/pages/Namespaces/namespacesPage.tsx`
Structurally mirrors the Clusters Drilldown: reached via `/namespaces/:cluster/:namespace/*` (both route params needed - namespace names aren't unique across clusters the way node names are, unlike the Nodes page's join-by-node-name shortcut). Title renders namespace name + a purple "namespace" `Badge` (`NamespacePageTitle`), with "in cluster X" underneath where X is a real-page-load link (`window.location`, not `<a href>`) back to the Clusters Drilldown - same variable-collision reasoning as `ClusterOverviewLinks` in `clustersApp.tsx`. Tabs: **Overview, CPU, Memory, Network, Storage, Logs, Events - all seven now have real content** (CPU/Memory/Network/Storage were built out this session; Logs/Events went from placeholder bar charts to real content the session before, then got their own dedicated tabs this session - see below). The old `getNamespacePlaceholderScene(title)` scaffold helper (a plain "X - coming soon" `Alert`) is gone entirely now that nothing references it.

**Overview tab**, top to bottom:
- "Namespace information" heading, then a health banner (`NamespaceHealthBanner` in `clusterOverviewCards.tsx`) - namespaces have no synthetic health-probe CronJobs like clusters do, so "healthy/degraded" is derived from firing-alert severity instead (`buildNamespaceAlertsSeverityQuery`). **Not built on `<Alert>`'s own `action` prop** - despite the doc comment "custom action element rendered in the alert's button area", `Alert`'s internal layout wraps `action` in a sibling `Stack` next to a title `Box` that's `flex: 1, minWidth: '50%'`, which pushes it to the far right with a large empty gap and no prop to change that. The banner is a hand-rolled flex row instead, reusing `theme.colors[severity]` tokens (the same ones `Alert` itself uses internally) for the tinted background/border/icon, with the alerts action button placed immediately after the message text via plain CSS `gap`.
- A row: info card (50% width - cluster link, workload-count link to Workloads, an EgressIP row) + two CPU/Memory "requests quota" cards (25% each, linking to the Resource Simulator pre-filtered to this namespace). **The quota cards reuse the Resource Simulator's own summary-card CSS classes directly** - `getStyles` was exported from `ResourceSimulatorObject.tsx` specifically so `NamespaceQuotaCard` (`src/scenes/namespaceOverviewCards.tsx`) could import the exact `summary`/`summaryHeader`/`summaryProgress*` classes instead of a look-alike reimplementation that could drift. Backed by `simulatorQuotaQuery()` from `resourceSimulator.ts` (the same `kube_resourcequota`-based query the Simulator itself uses) but *not* the Simulator's full baseline+scenario calculation - this just reads used/hard directly. **Sizing fixed this session**: the two quota `SceneFlexItem`s used to set `ySizing: 'content'` (their own natural, shorter height), leaving a visible gap below them relative to the taller info card - removed (default `ySizing: 'fill'`/`alignSelf: 'stretch'`) so they now stretch to match the info card's height, with the card's own content given `display: 'flex', flexDirection: 'column'` + `marginTop: 'auto'` on the help-text line so the extra height gets used (help text anchors to the bottom) instead of leaving dead space inside the card.
- EgressIP: **simplified in the 2026-08-18 session** (see that section) - no longer an OVN Prometheus metric at all. Now a direct RQLite CMDB query (`buildEgressIpQuery`/`buildEgressIpQueryTarget` in `namespaceOverviewQueries.ts`) keyed by the plain namespace name, combined into this same info card via Grafana's built-in Mixed datasource (`mixedDatasource()` in `queries/datasources.ts`) so the card still mixes a Thanos-backed row ("workloads:") with an RQLite-backed one ("egress ip:") without splitting into two cards. `InfoCard` (`clusterOverviewCards.tsx`) searches *every* query's frame for a row's field (not just `series[0]`) precisely to support this kind of mixed-source card.
- "Namespace optimization" heading + two timeseries (CPU/Memory): capacity (solid red)/limits (dashed red)/requests (dashed orange)/allocation (finely-dashed green, `dash: [2, 3]`)/usage (solid blue), matched by refId via `applyNamespaceOptimizationSeriesOverrides`.
- "Workloads" heading + a per-workload table, reusing the **exact same** combined meter cells (`usageTierCell`/`requestUsageCell` via `attachPercentField`) as the Namespaces list table and the ready/desired Pods cell from the Workloads page - Workload/Type/CPU Usage/CPU Requests/Mem Usage/Mem Requests/Mem Limits/Pods. Since this table is already scoped to one cluster+namespace (route params, not a picker), its queries group only by `(workload, workload_type)` - simpler than the top-level Workloads page's full `(cluster, namespace, workload, workload_type)` identity.
- "Logs / Events" heading, an "Only warn/error" `InlineSwitch`, then two stacked Elasticsearch bar charts (`Logs`/`Events`, grouped by `log.level`/`event.type`). See the dedicated section below - this went through several iterations.

**Elasticsearch Logs/Events design** (`src/queries/namespaceOverviewQueries.ts`): reads from a **new dedicated datasource variable** (`createLogsDatasourceVariable`, `LOGS_DATASOURCE_VARIABLE_NAME`) that's regex-filtered (`DataSourceVariable`'s own `regex` state field, matched against datasource *name* not uid) to just the three real log-management Elasticsearch instances the org actually wants these panels pointed at - it has many other Elasticsearch datasources configured that aren't relevant here.

Went through a real design correction mid-session: the *first* version used one Elasticsearch query per chart with a `terms` bucket-agg on the raw `log.level`/`event.type` field, color-matched case-insensitively via `matchFieldsWithNameByRegex` - which colored "ERROR"/"Error"/"Err" the same but still left them as **3 separate bars/series**, not what "group the log levels" actually means. Fixed by giving each **canonical** level (ALERT/ERROR/WARN/INFO/DEBUG/TRACE/Other for logs; Normal/Warning/Error/notice/Other for events) its **own** Elasticsearch query - a plain `date_histogram` count with a Lucene `OR` clause covering every known spelling/casing variant of that level, no `terms` agg at all - so same-meaning values genuinely merge into one summed series. "Other" is a `NOT (all known variants)` catch-all, colored grey. This also simplified the "Only warn/error" toggle: instead of swapping to a different combined query, it just changes *which* per-level queries are in the `SceneQueryRunner`'s `queries` array (`buildNamespaceLogsLevelQueries`/`buildNamespaceEventsLevelQueries(..., onlyWarnError)`).

The toggle (and the interval, see below) bypass Scenes' variable-interpolation machinery entirely - `CustomVariable`'s CSV/"label : value" option parser isn't a good fit for Lucene clauses full of colons and parentheses - and instead call `logsRunner.setState({ queries: [...] })` + `.runQueries()` directly from a small stateful React component (`LogsEventsLevelToggle`). `setState` alone does **not** trigger a re-run (that only happens automatically for *variable-driven* query changes); `runQueries()` must be called explicitly.

**Interval follows the timepicker with a 1-minute floor**, computed via `@grafana/data`'s `rangeUtil.calculateInterval(timeRange, resolution, '1m')` (the exported namespace, not a bare top-level export) - the same helper Grafana panels use internally for `$__interval`. Confirmed directly against a live instance (`curl /api/ds/query`) that Elasticsearch's `date_histogram` interval setting has **no** `">1m"`/minimum-floor syntax of its own here: `"auto"`, `"$__interval"`, and concrete durations (`"1m"`, `"10s"`) all return 200; `">10s"`/`">1m"` and `"$__interval_ms"` both 400. `LogsEventsLevelToggle` reads the scene's live time range via `sceneGraph.getTimeRange(logsRunner).useState()` and recomputes+re-runs both runners' queries in a `useEffect` keyed on `[timeRange.from, timeRange.to, onlyWarnError]` - both runners start with `queries: []` (a real no-op, not an empty request) since the interval isn't known until this effect's first run.

Bar charts: `StackingMode.Normal`, `showValue: VisibilityMode.Never` (per-bar value labels were cluttering a stacked multi-series chart), `xTickLabelRotation: -45` (a 1-hour range at a 1-minute floor is ~60 buckets - too many for horizontal labels not to overlap). Resolution passed to `calculateInterval` is 50, not the ~100 a timeseries panel would use, for the same "narrower panel, wider buckets" reason.

**Elasticsearch demo data**: `demo/seed/elasticsearch_seed.py` extended to also index `logmgmt.kind`/`k8s.cluster.name`/`k8s.namespace.name`/`log.level`/`event.type`-shaped documents (300 logs + 150 events, explicitly `PUT`-mapped as Elasticsearch `keyword` type before any document indexes them - **not** the default dynamic `text` mapping, which disables the fielddata a `terms`/`date_histogram` aggregation needs). The demo's Elasticsearch datasource was also **renamed** (`provisioning/datasources/demo.yaml`, `"Elastic (Demo)"` → `"Elasticsearch Logmanagement Prod Main"`) to match one of the three names the new datasource variable's regex expects - it had no matching datasource in the demo environment at all before this. **This session**, the datasource also gained `jsonData.logMessageField: 'message'`/`logLevelField: 'log.level'` (provisioning YAML change, needs a full Grafana container recreate per gotcha #4) so the new Logs tab's Log panel (below) shows colored level badges.

**Logs tab / Events tab** — `getNamespaceLogsScene`/`getNamespaceEventsScene` in `namespacesPage.tsx`, built out this session (previously placeholders; the Overview tab's bar charts above already existed). Each is a single `PanelBuilders.logs()` panel + its own "Only warn/error" `LogsTabLevelToggle`, reading a **raw log-line** Elasticsearch query (`metrics: [{ type: 'logs' }]`, no `bucketAggs`) rather than the Overview tab's per-level `date_histogram` counts - `buildNamespaceLogsQuery`/`buildNamespaceEventsQuery` in `namespaceOverviewQueries.ts`, reusing the same level-variant Lucene `OR`-clause building blocks. Simpler than the Overview tab's toggle: a Log panel needs no `$__interval`-style bucket size, so the toggle is just a straight query-string swap + `runQueries()`, no `useEffect`-on-live-time-range machinery. Confirmed via a direct `/api/ds/query` call that a "logs"-type ES query response already includes a `message` field (matches the seed data's own field name, no extra config needed for the message column) and that `preferredVisualisationType: 'logs'` is set automatically. The Overview tab's Logs/Events bar charts each gained a **Panel Link** (see below) pointing at their own dedicated tab.

**Panel Links** (`PanelLinkTitleItem`, new `src/scenes/panelLinks.tsx`) - a small link icon in a panel's title bar, wrapped in `Tooltip`, distinct from a field/data link (which is only clickable over the value/a cell, not the whole panel). Set via `panel.setState({ titleItems: <PanelLinkTitleItem .../> })` **after** `.build()`, since `VizPanelBuilder` has no `setTitleItems` - only the built `VizPanel`'s own `titleItems` state field. Originally added to the Kubernetes home page's Clusters/Nodes/Namespaces/Workloads stat tiles (alongside their existing Value Link) and then extracted to this shared file when the Namespace Overview tab's Logs/Events bar charts needed the identical pattern - avoids a `kubernetesOverviewScene.tsx` ↔ `namespacesPage.tsx` circular import.

**CPU tab / Memory tab** — `namespaceCpuScene.tsx`/`namespaceCpuQueries.ts` and `namespaceMemoryScene.tsx`/`namespaceMemoryQueries.ts`, built out this session, mirroring the already-built Cluster Drilldown CPU/Memory tabs' layout (3 stat panels row → Pod filter → 3-timeseries row → Workloads table) but scoped to one namespace via a **new Pod filter variable** (`createPodFilterVariable(clusterRegex, namespaceRegex)` in `datasourceVariables.ts`, same "literal cluster/namespace inlined, no scene-level variable to reference" pattern as `createNodesFilterVariable` - this page has no scene-level `pod` variable to point at, it's the drilldown route's own scope). Panels: 3 stat tiles (Alerts: Firing / Scheduling: Containers with requests set / Alignment: Usage-Requests, all "(p95)"-labeled but - matching the existing `buildEfficiencyStatPanel` convention on the Cluster tab - not literally `quantile_over_time`'d, just a plain ratio with a sparkline), "Overview: Usage" (limits/requests/usage, refId-matched red-dashed/orange-dashed/blue-solid styling reused from `applyNamespaceOptimizationSeriesOverrides`'s convention but redeclared locally per-file to avoid a circular import), "Distribution: Workload usage" (stacked), "Alignment: Workload Usage/Requests (%)", and a Workloads table reusing the **exact same** `namespaceWorkloadsTableQueries.cpu_*`/`mem_*` queries the Overview tab's own Workloads table already runs (byte-for-byte identical PromQL, just imported instead of duplicated). Memory's table additionally has a Mem Limits column (CPU has none - no container in this demo has a CPU limit set, by design, see the Efficiency tab notes). **Legend conventions, set via explicit follow-up requests (don't assume one rule covers all four new tabs - each landed slightly differently)**: CPU/Memory's 3 timeseries panels use `LegendDisplayMode.Table` + `calcs: ['p95']` (a single "95th %" value column) and series named "Sum of container CPU/Memory limits/requests/usage" (Overview: Usage) / `{{workload_type}}/{{workload}}` (Distribution/Alignment). Network's 4 panels use `calcs: ['min', 'p90', 'max']` (a 3-column Min/90th %/Max table) - a *different* percentile than CPU/Memory's, requested separately. Storage never got an explicit legend follow-up, so its 8 multi-series panels kept whatever the Cluster Drilldown Storage tab's own sibling queries already used: `LegendDisplayMode.List` + `calcs: ['lastNotNull']`.

**Network tab** — `namespaceNetworkScene.tsx`/`namespaceNetworkQueries.ts`, built out this session, mirroring the Cluster Drilldown's own Network tab (rx-positive/tx-negative two-series-per-panel via a literal `- sum(...)` baked into the tx query text, no stacking mode) but with **no Pod filter** - none of these queries reference a `$pod` placeholder, unlike CPU/Memory/Storage. 4 panels: Bandwidth, Saturation (dropped packets), and "by workload" versions of both (using a bespoke 4-branch bare-pod/static-pod/replicaset/attributed-workload fallback chain pasted verbatim from the given queries - a simpler, separately-maintained cousin of `kubernetesTopStatQueries.workloads`'s own 8-branch version, not reused since the branch sets genuinely differ). Legends (final, per follow-up): "Network Bandwidth"/"Network Saturation" panels keep plain "Receive"/"Transmit" (Saturation: "Receive/Transmit dropped packets"); the two "by workload" panels use "Receive/Transmit ({{workload_type}}/{{workload}})" (Saturation: "...dropped pakets..." - literal spelling as given by the user, not "fixed" to "packets"); all four panels get a Min/90th %/Max legend table.

**A real, pre-existing demo-data bug found and fixed while building the Network tab's "by workload" panels**: `namespace_workload_pod:kube_pod_owner:relabel`'s row for the bare `debug-shell-aws`/`debug-shell-gce` pods used a shared, invented `workload="debug-shell"` label (dropping the per-cluster suffix, same convention as the *controller-owned* workloads like `"app"`), but `kube_pod_owner{owner_kind=""}` (independently referenced by several existing queries, including this new Network tab query's own fallback branch) derives `workload` from the pod's own **unmodified** name via `label_replace`, i.e. `"debug-shell-aws"` - a genuine disagreement between two metrics about the same pod's workload identity. This was harmless everywhere it was previously only combined via plain `OR` (Namespaces page, `kubernetesTopStatQueries.workloads`), but Prometheus's `* on (...) group_left(...)` binary-operator join (which several "by workload" queries use, including this new one) **requires uniqueness on the join's right-hand side** - having both labels appear for the same pod inside that specific combined-then-joined pattern made Prometheus hard-fail with `"many-to-many matching not allowed"` the moment the Network tab's queries (the first ones in this codebase to combine `namespace_workload_pod:kube_pod_owner:relabel` **OR**-ed with `kube_pod_owner{owner_kind=""}` and *then* `group_left`-join the result) ran. **Fixed** by renaming `namespace_workload_pod:kube_pod_owner:relabel`'s debug-shell rows to match the pod's own full name (`workload="debug-shell-aws"`/`"-gce"`, no longer a shared `"debug-shell"`) - as a side effect, this also fixed a **previously-unnoticed but already-visible** bug on the Workloads page: `merge`-ing `ready_pods`/`desired_pods` (which only ever derived `debug-shell`'s workload from `kube_pod_owner{owner_kind=""}`, i.e. the full-pod-name version) against `cpu_usage`/`mem_usage` (which derive it from `namespace_workload_pod:kube_pod_owner:relabel`, the shared-name version) couldn't combine on a `workload` field that disagreed between the two, silently splitting debug-shell into two incomplete table rows (one with Pods data and no CPU/Mem, one with CPU/Mem and no Pods) - confirmed via screenshot that it's now one clean row with both.

**Storage tab** — `namespaceStorageScene.tsx`/`namespaceStorageQueries.ts`, built out this session, mirroring the Cluster Drilldown's Storage tab layout (Ephemeral Volume Usage → PVC Storage Class → Volume Bytes/inodes + by-workload pairs → PVC/PV Status → Throughput/IOPS + by-workload pairs) with a Pod filter (used by Ephemeral Volume Usage and the Throughput/IOPS "by workload" pair only - every PVC-related query has no `$pod` placeholder at all, since a PVC isn't filtered by which pod currently mounts it in these particular queries). The "by workload" Throughput/IOPS queries reuse a **third, pod-filtered** variant of the bare-pod/static-pod/replicaset workload-attribution fallback chain (`workloadAttributionWithPodFragment` in `namespaceStorageQueries.ts` - distinct from the Network tab's pod-less version, since every branch here also carries `pod=~"$pod"`).

**Two more real demo-data gaps found and fixed** (distinct from the Network tab's bug above - these were genuinely-missing data, not a naming conflict): (a) `kube_pod_spec_volumes_persistentvolumeclaims_info` - the join key **every** PVC-related query in this tab starts from (`group by (cluster, namespace, pod, persistentvolumeclaim, volume) (...)`, then `* on (cluster, namespace, persistentvolumeclaim) group_left(...)` against the actual PVC metrics) - **didn't exist anywhere in the demo dataset at all**, so 8 of this tab's 12 panels showed "No data" regardless of the underlying PVC metrics (`kube_persistentvolumeclaim_info`, `kubelet_volume_stats_*`, etc.) already having real values. Added, deliberately mounted on the real `app` workload pod (`app-aws`/`app-gce`) rather than the pre-existing standalone `app-0` pod the Ephemeral-Usage/Throughput/IOPS metrics use, so the PVC is attributable to a real established workload for the "by workload" panel. (b) `kube_persistentvolume_status_phase`-fed "PV Status" panel's own PVC→PV join needs `kube_persistentvolumeclaim_info{volumename!=""}`, but the existing demo row had no `volumename` label at all (missing label = empty string = filtered out entirely) - added `volumename="pv-data-pvc"` linking it to the existing `kube_persistentvolume_status_phase{persistentvolume="pv-data-pvc"}` row.

**Also extended `container_fs_{reads,writes}_{bytes_,}total`** (4 metrics) with rows for the 5 real workload pods per cluster, on top of - not replacing - the pre-existing standalone `app-0` pod's rows: the Cluster Drilldown's own Storage tab's Throughput/IOPS-by-namespace panels only ever needed `app-0`'s data (no workload join), but the new Namespace Storage tab's "by workload" pair does need workload-attributable rows, which `app-0` (no `namespace_workload_pod:kube_pod_owner:relabel`/`kube_pod_owner` row of its own) can't provide.

### Workloads (list page) — `src/pages/Workloads/workloadsPage.tsx` + `src/queries/workloadQueries.ts`
Table across all selected clusters/namespaces: Cluster/Namespace/Workload/Type/**Pods**/CPU Usage/Requests/Mem Usage/Requests/Limits (CPU/Mem columns **redesigned this session** to the same combined value+percent+bar cells as the Namespaces list table - `usageTierCell`/`requestUsageCell` via `attachPercentField`, replacing the previous separate "value" + "value %" column pairs; same CPU-Usage-by-Requests / Mem-Usage-by-Limits coloring convention). A workload's identity is `(cluster, namespace, workload, workload_type)` — genuinely multi-dimensional — so this **also uses `merge`**, every one of its 10 queries keeping its full original grouping verbatim.

**Pods column** (`readyDesiredPodsCell` in `tableCells.tsx`) shows `ready / desired` text over a proportional colored bar, matching Grafana Play's combined column. **Fixed a real bug this session**: the field carrying "desired" used to stay in the frame with a `hideFrom: { viz: true, ... }` override meant to hide its own column while keeping it readable for this cell — that override **never actually worked** on the Table panel (see the dedicated gotcha below). It's now attached directly onto the "ready" field's own `config.custom` via a generalized `attachFieldValues`/`attachDesiredPodsField` helper (also the basis for the Namespaces `attachPercentField` above), and the raw "desired" field is fully `excludeByName`'d instead of merely (attempted-to-be) hidden. Readiness coloring stays **inverted** from the usual usage-tier scale (more-ready-than-desired is green, less is red — plain colors, not `usageTierFromFraction`).

**Workload filter variable** (`createWorkloadFilterVariable` in `datasourceVariables.ts`): a `Workload` picker sits alongside Cluster/Namespace, row-filtering client-side via a `filterByValue` transform with `${workload:regex}` interpolated into its options (confirmed `SceneDataTransformer` really does interpolate scene variables into transformation `options`, not just query expressions).

**Multi-cluster gotcha inherited from the given queries (real, not just a demo issue)**: the cpu/mem queries join via `* on(namespace, pod) group_left(workload, workload_type) namespace_workload_pod:kube_pod_owner:relabel{...}` — `cluster` is *not* in the `on(...)` clause, so identical pod names across two different selected clusters make Prometheus hard-fail with `"many-to-many matching not allowed"`. Property of the queries as given, not "fixed" per the usual literal-translation rule.

### Workloads Drilldown — `getWorkloadDetailPage` in `src/pages/Workloads/workloadsPage.tsx`
Built out entirely in the 2026-08-18 session (see the dedicated section near the end of this file for the full turn-by-turn). Reached via `/workloads/:cluster/:namespace/:workloadType/:workload/*` — **four** route params, not two/three like the other drilldowns, since a workload's identity is genuinely `(cluster, namespace, workload, workload_type)` (see the Workloads list page's own note above). Title renders workload name + a green workload-type `Badge` (`WorkloadPageTitle`, `formatWorkloadTypeLabel`/`WORKLOAD_TYPE_LABELS` in `workloadOverviewQueries.ts` - Deployment/StatefulSet/DaemonSet/Job/Pod/Static Pod/ReplicaSet), "in cluster X" subtitle linking back to the Clusters Drilldown. **All 7 tabs (Overview, CPU, Memory, Network, Storage, Logs, Events) have real content** - structurally one level deeper than the Namespace Drilldown's own tabs (scoped to a single already-known workload's own pods, not a namespace's workloads/pods).

**The defining architectural difference from every other drilldown page**: since the exact workload identity is already known from the route (unlike the list-level Workloads table, which has to cover *every* workload via big OR-chains/fallback branches), every query here reads the workload's own kind-specific kube-state-metrics metric directly - no fallback chain needed. E.g. ready/desired replicas (`buildWorkloadReadyDesiredQueries`) select `kube_deployment_status_replicas_available`/`kube_deployment_spec_replicas` directly for a `workloadType === 'deployment'`, rather than the Workloads table's own giant `OR`-chain across every kind.

**Hidden Pod variable, not a picker**: most metrics/logs/events don't carry a dedicated "workload" label (kube-state-metrics resource metrics use `pod`; logs/events use `orchestrator.resource.name`), so most queries filter by pod instead. Since this page has nothing left to filter (it's already scoped to exactly one workload), every tab creates its **own hidden** `Pod` variable via `createPodFilterVariable(clusterRegex, namespaceRegex, { workload })` (extended this session with an optional `workload` param - when given, its underlying query switches from `kube_pod_info` to `namespace_workload_pod:kube_pod_owner:relabel{...,workload="X"}`, since only the latter has a `workload` label at all) + `.setState({ hide: VariableHide.hideVariable })`, then interpolates `${pod:regex}` into every `$pod`-referencing query via `substituteWorkloadTokens` (`workloadOverviewQueries.ts` - also handles `$cluster`/`$namespace`/`$workload` as literal JS substitutions, same as every other drilldown page's own `substitute()` helper). Each tab is its own `EmbeddedScene`, so this hidden variable is recreated per-tab, not shared.

**Overview tab** (`getWorkloadOverviewScene`): "Workload information" heading, an alerts-derived health banner (`NamespaceHealthBanner`, generalized this session with an optional `subject` prop - defaults to `'Namespace'`, this page passes `'Workload'`) filtered to just this workload's own pods via a join against `namespace_workload_pod:kube_pod_owner:relabel`, two info cards mirroring the Cluster Drilldown's own two-info-box row (not the Namespace Drilldown's info-card+2-quota-cards layout) - left: cluster/namespace (linking to their own Drilldowns, via `InfoCard`'s new `render(frames)` row callback rather than `fieldName`, since these values are already known from route params)/egress ip; right: ready replicas ("1 / 1" text, `InfoCardRow.render` combining two fields)/create date (`dateTimeFromNow` unit, needed new `kube_*_created` demo metrics - didn't exist in the demo dataset at all)/workload type. Then "Workload optimization" (two timeseries, allocation/limits/requests/usage, same styling as the Namespace/Cluster Drilldowns' own optimization charts minus a "capacity" line - a workload has no resourcequota-style ceiling), "Pods" (a table, one row per pod, STATUS shows a waiting-container reason like ImagePullBackOff in place of the plain phase when one exists via a new `podStatusCell`, CPU/Memory Requests combine with their "%" into one `requestUsageCell` same as the Namespaces/Workloads list tables - Memory Limits stays a separate colored-percent column), "Logs / Events" (same per-canonical-level bar charts as the Namespace Drilldown's own Overview tab, scoped via an `orchestrator.resource.name` wildcard clause added to `buildWorkloadLogsLevelQueries`/`buildWorkloadEventsLevelQueries`).

**`InfoCard` generalized this session** (`clusterOverviewCards.tsx`): `InfoCardRow.fieldName` is now optional, replaced by an optional `render(frames: DataFrame[]) => string` callback for rows whose value is already known outright (cluster/namespace from route params) or combines multiple fields (ready/desired). `findFieldAcrossFrames(frames, fieldName)` was extracted out of the renderer as its own export so `render` callbacks can reuse the same multi-frame search.

**CPU tab / Memory tab** (`workloadCpuScene.tsx`/`workloadMemoryScene.tsx` + their own `*Queries.ts`): mirror the Namespace Drilldown's own CPU/Memory tabs one level deeper - 3 stat tiles, "Overview: Usage", "Distribution: Pod usage" (stacked, legend `{{workload_type}}/{{pod}}` - swapped from the namespace-level tabs' `{{workload_type}}/{{workload}}` since pod is now the varying dimension), "Alignment: Pod Usage/Requests (%)", and a **"Pods"** table (POD/TYPE/REQUESTS/USAGE (P95)/USAGE-CAPACITY (P95, %) - all 5 kept as separate columns, *not* combined, since this table's own column list was given explicitly as 5 distinct columns unlike the Overview tab's Pods table). Memory's Pods table merges by `(cluster, namespace, pod, container)` rather than just `pod` - only its `timeline` query carries `workload`/`workload_type`/`image_spec` at all, given verbatim.

**Network tab** (`workloadNetworkQueries.ts`/`workloadNetworkScene.tsx`): Bandwidth/Saturation + "by pod" pairs - no bare-pod/static-pod/replicaset fallback chain needed here (unlike the Namespace Drilldown's own Network tab), since `$pod` already resolves to exactly this workload's own pods.

**Storage tab** (`workloadStorageQueries.ts`/`workloadStorageScene.tsx`): same layout as the Namespace Drilldown's Storage tab, fewer series on the Volume Bytes/inodes panels (no "filling up" alert-marker series were given this time). **Fixed an apparent copy-paste typo** in the given "IOPS by pod" Tx query (reused `container_fs_reads_total` instead of `container_fs_writes_total` - every sibling Tx line, including this same panel's own non-"by pod" version, correctly uses `writes`).

**Logs tab / Events tab**: single raw-log-line Log panel each + "Only warn/error" toggle, same as the Namespace Drilldown's own dedicated tabs. Extracted `LogsTabLevelToggle`/`buildLogPanel` out of `namespacesPage.tsx` into a new shared `src/scenes/logPanels.tsx` (and, separately, the Overview tab's own bar-chart toggle `LogsEventsLevelToggle` into `src/scenes/logsEventsLevelToggle.tsx`, generalized to take `buildLogsQueries`/`buildEventsQueries` callbacks instead of a hardcoded cluster/namespace pair) so both pages share one implementation of each toggle instead of duplicating them.

**Workloads table's own Workload column now links here** (`${WORKLOADS_URL}/${cluster}/${namespace}/${workload_type}/${workload}`), same as its Cluster/Namespace columns. The Namespace Drilldown's own Overview/CPU/Memory "Workloads" tables' Workload columns link here too (added as a follow-up fix - see the session section).

### Pod Drilldown — `getPodDetailPage` in `src/pages/Pods/podsPage.tsx` (new this session, v1.10.0)
One level deeper than the Workload Drilldown, reached from its own Overview tab's Pods table. `src/pages/Pods/` is a new directory (own `podCpuScene.tsx`/`podMemoryScene.tsx`/`podNetworkScene.tsx`/`podStorageScene.tsx` files, split out from day one rather than growing `podsPage.tsx` the way `namespacesPage.tsx`/`workloadsPage.tsx` did before their own splits). Reached via `/workloads/:cluster/:namespace/:workloadType/:workload/pods/:pod/*` - a `drilldowns` entry nested on the *Workload* Detail page's own returned `SceneAppPage` (confirmed against `@grafana/scenes`' compiled `SceneAppPage.js` that `tabs` and `drilldowns` on the same page both just feed routes into the same internal `<Routes>`, no conflict). Title renders pod name + an orange "pod" `Badge`, "in cluster X" subtitle. **All 7 tabs (Overview, CPU, Memory, Network, Storage, Logs, Events) have real content.**

**The defining pattern, one level deeper than the Workload Drilldown's own**: every CPU/Memory/Network/Storage query is the Workload Drilldown's own query **reused verbatim** (imported straight from `workloadCpuQueries.ts`/`workloadMemoryQueries.ts`/`workloadNetworkQueries.ts`/`workloadStorageQueries.ts` - no new Pod-level query files for these four tabs) - every one already carried its own `pod=~"$pod"` filter (confirmed against source before starting), so the only thing that changes is what `$pod` resolves to: the Workload Drilldown needs a live *variable* (every pod belonging to the workload), this page already knows exactly one pod from its own route params, so `$pod` is substituted with that pod's own literal escaped name directly via the existing `substituteWorkloadTokens` - **no hidden Pod variable needed at all** at this level, unlike every tab one level up.

**Overview tab** (`getPodOverviewScene`): "Pod information" heading, an alerts-derived health banner (`NamespaceHealthBanner`, `subject: 'Pod'`), two info cards - left: status (`kube_pod_status_phase`, colored green/orange/red by phase)/pod start time (`kube_pod_start_time`, new demo metric, `dateTimeAsIso` unit → `YYYY-MM-DD HH:mm:ss` per an explicit follow-up, not the usual relative `dateTimeFromNow`)/number of restarts (colored green/orange/red by the same `>2` threshold the Kubernetes home page's own "Restarting containers" panel uses); right: cluster (linked)/node/namespace (linked). Then "Pod optimization" (two timeseries, allocation/limits/requests/usage, same styling as the Workload Drilldown's own - no capacity line, a pod has no resourcequota-style ceiling), a "Containers" heading + table (added as a follow-up - combines the CPU tab's and Memory tab's own per-container columns into one: CONTAINERS/IMAGE_SPEC/CPU Usage/CPU Requests/Mem Usage/Mem Requests, same `requestUsageCell`/`usageTierCell` combined-cell convention as every other Pods/Containers table in this app), and "Logs / Events" (same per-canonical-level bar charts as the Workload Drilldown's own Overview tab, scoped via `orchestrator.resource.name` matching the pod's **exact** name instead of a workload-name wildcard prefix - confirmed via the demo Elasticsearch seed script that this field genuinely holds pod names, so an exact match is more correct one level down, not just simpler).

**`InfoCard` gained an optional per-row `color` callback** (`clusterOverviewCards.tsx`, `(frames, theme) => string | undefined` - takes the live theme too, since a row callback isn't itself a React component and can't call `useTheme2()` on its own) - used by the status/restart-count rows above.

**Gotcha found building the info cards**: a plain instant Prometheus query's "Value" field is *never* disambiguated to `Value #<refId>` unless the query also sets `format: 'table'` - confirmed via network capture that a bare instant query stays literally named "Value" forever regardless of how many sibling queries share the runner, and a label like "phase"/"node" never splits into its own scalar column for a plain `findFieldAcrossFrames(frames, 'phase')` lookup either - it stays attached as the Value field's own `.labels` instead. Every `InfoCard`-feeding query on this page (and the Node Drilldown's own, built later this session) now sets `format: 'table'` for this reason. **This was already a latent, unfixed bug on the Workload Overview tab's own "ready replicas"/"create date" rows** (both instant, neither `format: 'table'`) - worth revisiting if that's ever reported broken, though it wasn't touched this session (out of scope for a Pod Drilldown change).

**CPU tab / Memory tab** (`podCpuScene.tsx`/`podMemoryScene.tsx`): 3 stat tiles, "Overview: Usage", "Distribution: Pod usage" (now just a single-series chart - genuinely one pod), "Alignment: Pod Usage/Requests (%)", and a table **renamed "Pods" → "Containers"** (a follow-up ask) - the identity column now shows the *container* name instead of the (now-redundant, already-known-from-the-page) pod name, via a **new shared query** `podContainerInfoQuery` (`podOverviewQueries.ts` - identical text to `workloadMemoryPodsTableQueries.timeline`, since that one already carried container/image_spec; the CPU table's own equivalent didn't, so this page swaps it in for CPU too instead of reusing `workloadCpuPodsTableQueries.timeline`). TYPE column dropped, IMAGE_SPEC added (the deployed image), REQUESTS/USAGE-CAPACITY-% combined into one `requestUsageCell` (previously two separate columns, matching the Namespaces/Workloads list tables' own convention - "put back together" per an explicit follow-up ask).

**Network tab / Storage tab**: same verbatim-reuse pattern, no query changes needed beyond substituting `$pod` with the literal pod name.

**Logs tab / Events tab**: single raw-log-line Log panel each + "Only warn/error" toggle, matching the exact-pod-name query shape described above. New `buildPodLogsLevelQueries`/`buildPodEventsLevelQueries`/`buildPodLogsQuery`/`buildPodEventsQuery` added to `namespaceOverviewQueries.ts` (alongside the existing Namespace-/Workload-level ones, since the shared Lucene/level-def helpers they need are private to that file).

**Workload Overview tab's own Pods table's POD column now links here** (`${WORKLOADS_URL}/${cluster}/${namespace}/${workload_type}/${workload}/pods/${pod}`), same pattern as every other "column links to its own Drilldown once it exists" addition in this app.

### Alerts (top-level page) — `src/pages/Alerts/alertsPage.ts` + `investigateActionCell.tsx`
Kubernetes-icon + "Alerts" title, datasource picker + `cluster`/`node`/`namespace`/`severity`/`alertname` variable pickers + timepicker. 3 timeseries panels (firing-by-cluster, firing-by-namespace, alert severity) + a table with a synthetic **Action** column driving an `OpenAssistantButton` ("Investigate"). All 3 timeseries panels have the per-panel comparison control.

**The user's org runs a custom, in-house-forked assistant app** (https://github.com/elohmeier/grafana-pi-app, default plugin id `g42-pi-app`). Three upstream bugs found and fixed there across earlier sessions, all in `Lprod`'s fork, PR'd against `elohmeier/grafana-pi-app` — PRs [#30](https://github.com/elohmeier/grafana-pi-app/pull/30), [#31](https://github.com/elohmeier/grafana-pi-app/pull/31), [#32](https://github.com/elohmeier/grafana-pi-app/pull/32), all merged. **If "the Investigate button doesn't work" comes up again**, check in order: (a) which plugin id is installed (`g42-pi-app` vs `grafana-assistant-app`), (b) whether the deployed `grafana-pi-app` version includes all three fixes, (c) whether `extensionSidebar` feature toggle is enabled, (d) whether the alert *fields themselves* are populated (a `renameByName`-vs-`field.name` bug — `organize`'s `renameByName` only sets display name, never the underlying `field.name`) as distinct from whether the sidebar/prompt mechanism fires at all.

### Per-panel timeseries comparison — `src/scenes/panelTimeRangeCompare.tsx`
`PanelTimeRangeCompare` (a `SceneTimeRangeCompare` subclass), passed to `PanelBuilders.timeseries()...setHeaderActions(new PanelTimeRangeCompare())` on every timeseries panel on Clusters Drilldown and Alerts (not the table-only pages — no timeseries there). Scoped per-panel because `SceneQueryRunner.getClosestExtraQueryProviders()` walks up the scene graph checking each ancestor *and its direct children*, and `setHeaderActions()` registers its argument as a real scene-graph child of the `VizPanel` — so an instance there only affects that one panel's query, not the whole page. Fixed option set (None/Hour/Day/Week-before) via reassigning `this.getCompareOptions` after `super()` in the subclass constructor (an own-instance arrow-function property, cleanly shadowable). "None" has to be a real menu entry (value `'__noPeriod'`, the internal sentinel `SceneTimeRangeCompare` checks — not exported from `@grafana/scenes`, inlined and verified against the compiled source) since `hideCheckbox: true` removes the checkbox that would otherwise let you turn it off.

**Visual style went through three iterations**, each a direct user reaction to the previous one:
1. First pass: the base class's default `ButtonGroup`+`ButtonSelect` — read as a full toolbar control, too heavy for a panel header.
2. Restyled as a plain `<Button variant="secondary" size="sm">` — matched Grafana Play's own "Predict CPU usage" button (inspected directly on play.grafana.org via Playwright).
3. **Current**: restyled again as a small colored **Badge pill** (`@grafana/ui`'s `Badge`, not `Button`) — matching this app's *own* Resource Simulator quota-card badges (`StatusBadge` in `ResourceSimulatorObject.tsx`, itself just a `Badge`) more closely than Grafana Play's button did. Since `Badge` isn't natively clickable (no `onClick` in its props, not `forwardRef`), it's wrapped in an unstyled `<button>` that `Dropdown` clones its click/ref handlers onto, with the (non-interactive) `Badge` rendered inside. Blue when idle ("Compare"), purple when a period is selected (its label, e.g. "Week before").

Demo-stack caveat: the local Prometheus only retains a shallow, discontinuous data window, so Day-before/Week-before comparisons won't show an overlay line there yet — not a bug.

### Resource Simulator — `src/pages/ResourceSimulator/`
`ResourceSimulatorObject.tsx` was ~2400 lines, mixing ~20 pure formatting helpers (`formatCores`, `formatValue`, `formatMeterPair`, ...) in with a dozen+ React components. **Split the formatters out into `resourceSimulatorFormatters.ts` this session** (a graphify-flagged "overly large file" finding) — `WORKLOAD_TYPE_OPTIONS`/`UsageMeterUnit` moved there too since `formatWorkloadType`/`formatMeterPair` need them and moving only the formatters would've created a circular import (component code calling into formatters, formatters needing types/constants only the component file had). Verified via Playwright: base page render + adding a temporary workload row, both unchanged.

**"No workload rows" empty state polished**: was a plain `<Alert severity="info">` text block, now a proper empty state (dashed-border box, `layer-group` icon, bold title, muted description) matching the rest of the app's visual language.

**Quota cards** (top of page — "CPU requests quota", "Unlimited" badges, etc.) are the visual reference other pages now borrow from: `StatusBadge` is literally just `@grafana/ui`'s `Badge`, and `UsageRequestMeter`'s used/requested-with-bar layout is what `requestUsageCell` (Namespaces, `tableCells.tsx`) is modeled on.

### graphify — project-scoped Claude Code skill, actually used this session
`.claude/skills/graphify/` (github.com/Graphify-Labs/graphify). A previous session's summary noted `graphify-out/graph.json` didn't exist yet — this session built it (scoped to `src/`, code-only corpus, no LLM/semantic extraction needed) and used `graphify query`/the God-Nodes report to drive an actual code-review pass: flagged `ResourceSimulatorObject.tsx` (2400+ lines) and the duplicated `usageThresholds` as the two concrete findings, both fixed (see above). `.claude/settings.json`'s `PreToolUse` hooks before Bash/Grep/Read fired constantly and repeatedly through the rest of the session (not just during the initial review) — they're a *soft* nudge (CLAUDE.md says so explicitly), satisfied with a quick `graphify query` before continuing rather than treated as a hard gate on every single subsequent Read/Bash call. Run `graphify update .` after modifying code to keep it current (scope it to `src/` specifically if re-running detect/extract by hand rather than via the `/graphify` skill entry point, to avoid pulling in `node_modules`/`demo`/build output).

### Not attempted / explicitly deferred
- **Predictions/Actions column** (Grafana Play's Nodes table) — discussed, not built.
- Splitting `clustersApp.tsx` (1800+ lines, the second-largest file after the Resource Simulator split) by tab — proposed as a follow-up optimization, not done.
- Sparkline trend indicators in table cells, loading skeletons for the list-page tables — proposed as style suggestions, not implemented (see "loading states" gotcha below for why the skeleton idea specifically turned out to be low-value).

## Known gotchas discovered this/previous sessions (read before repeating work)

1. **Grafana's `renameByName` (in the `organize` transform) only sets a field's display name, never its underlying `field.name`.** Any custom cell renderer or code that looks up `frame.fields.find(f => f.name === X)` must use the pre-rename name.
2. **`joinByField` is only safe when the join key is a single field that's actually unique across everything being combined.** When the natural identity is multi-field, use Grafana's `{ id: 'merge', options: {} }` transform instead.
3. **Cross-page filter variable propagation.** `SceneAppPage`'s `$variables` are constructed eagerly for every top-level page at once. Each filter variable re-syncs its value from the raw URL query param **on activation**, never from the constructor, and is excluded from every top-level page's `preserveUrlKeys`.
4. **`plugin.json` changes need a full Grafana container recreate**, not just a rebuild/restart. Pure code/panel changes only need `podman restart`.
5. **Webpack build cache goes stale** with the `node_modules`-symlink shortcut — `rm -rf node_modules/.cache/webpack` before trusting a build.
6. **Local demo containers bind-mount files from whatever worktree last recreated them, not necessarily this one.** `podman inspect <container> --format '{{range .Mounts}}...'` before assuming a container is serving *this* worktree's files. Recreate (not just restart) after repointing a bind mount to a different worktree's `dist/`/`demo/` paths — confirmed again this session for `kube-metrics-demo` after editing the demo metrics file.
7. **Adding a genuinely new npm dependency requires a real `npm install`, not the symlink trick** — and if the shared `node_modules` belongs to a checkout pinned to an older commit missing a dependency your worktree declares, do an independent `npm ci` in your own worktree instead of installing into the shared one.
8. **`joinByField` needs `mode: 'inner'`, not the default `'outer'`, when one of the joined queries is meant to act as an allow-list filter.**
9. **Grafana's built-in reducer/calc ids often already say exactly what's needed, verbatim** — check `@grafana/data`'s `fieldReducer.mjs` before inventing a custom label.
10. **A flexbox `SceneFlexItem` with `xSizing: 'fill'` plus `minWidth: 0` still won't guarantee equal-width siblings if one contains a long unbroken string** — set an explicit `width: '50%'` instead.
11. **This local Prometheus's `kube-metrics-demo` scrape job needs `honor_labels: true`** so demo metrics that embed their own `job` label aren't overwritten by the scrape job's name.
12. **A `Nodes: Standalone plugin pages` Helm config option can hide specific app pages from the Grafana nav without touching plugin code** (`navigation.app_standalone_pages`) — nav-tree cosmetics only, not access control.
13. **Grafana Scenes' `SceneQueryRunner` discovers `ExtraQueryProvider`s by walking *up* the scene graph from its parent, checking each ancestor and that ancestor's direct children** (`getClosestExtraQueryProviders`). A provider doesn't have to live in a page's top-level `controls` — placing one as a `VizPanel`'s `headerActions` scopes it to that single panel. Useful pattern for any future "per-panel control" need.
14. **`SceneDataTransformer` interpolates scene variables into a transformation's `options`**, not just query expressions (`JSON.stringify` → `sceneGraph.interpolate` → `JSON.parse`). A `${var:regex}` token anywhere inside e.g. a `filterByValue` transform's `options` resolves correctly before the transform runs.
15. **Grafana's `match[]` HTTP parameter (used by `label_values(query, label)`) only accepts a plain Prometheus series selector**, not aggregations/`label_replace`/`OR`/`group_left`. Use `query_result(<expr>)` instead when the values only exist after PromQL processing, paired with a `regex` field on the `QueryVariable`.
16. **Grafana's `query_result(...)`/`label_values(...)` wrapper-detection in template variable queries doesn't span newlines.** A multi-line, `#`-commented PromQL expression passed to `query_result(...)` isn't recognized as the wrapper at all — falls through to the `match[]` endpoint with the same failure as #15. Strip comments and join lines with spaces first.
17. **This project's checked-in code is *not* Prettier-formatted** — lines up to ~400 characters exist by design. Running a blanket `npx prettier --write` on an edited file silently reformats large amounts of unrelated pre-existing code, polluting the diff. Hand-format only the lines you actually changed.
18. **Playwright's Chromium needs several system shared libraries that may not be installed, and `playwright install --with-deps` needs root.** If `sudo` isn't available, `apt-get download <package>` still works without root — extract with `dpkg-deb -x`, run with `LD_LIBRARY_PATH=<dir>/usr/lib/x86_64-linux-gnu` prepended.
19. **A `custom.*` field-config override — e.g. `overrideCustomFieldConfig('hideFrom', { viz: true })` — silently does nothing on Grafana's Table panel, even though the Table panel's own rendering code (`Table/utils.mjs`, `TableNG/utils.mjs`) checks `field.config.custom.hideFrom?.viz`.** Root cause (confirmed by reading `@grafana/data`'s `applyFieldOverrides`/`setDynamicConfigValue`): a `custom.*` override only survives if the *active panel type* has that property registered in its own field-config registry (`context.fieldConfigRegistry.getIfExists(value.id)`); an override for an unregistered id is dropped with **no error, no warning** — it just never reaches `field.config`. Table doesn't register `hideFrom` as an editable custom option, so the rendering-side check for it is effectively dead code for this panel type, even though it's the exact mechanism Grafana's own legend "hide series" feature uses successfully on other panel types (`seriesVisibilityConfigFactory` in `@grafana/scenes`, which sets `custom.hideFrom` too). **Diagnosed by temporarily swapping the property for `overrideDisplayName` on the same field/matcher block — that one DID take effect, isolating the problem to `hideFrom` specifically, not the override mechanism in general.** Bit both the Workloads Pods column (desired-pods field) and would have bitten the Namespaces redesign if reused. **Fix: don't try to hide the field via override — attach its value onto a sibling field's own `config.custom` (a small `CustomTransformOperator`, e.g. `attachFieldValues`/`attachDesiredPodsField`/`attachPercentField` in `tableCells.tsx`) and fully `excludeByName` the raw field via `organize` instead.** This pattern is now the standard way to do "value needed by another cell, but shouldn't be its own column" in this codebase — reach for it directly next time instead of trying `hideFrom` first.
20. **Investigated whether list-page tables need an explicit loading skeleton/spinner — concluded no code change was warranted.** Delaying only `/api/ds/query` requests via `page.route()` (Playwright) while leaving everything else at normal speed showed the actual per-panel loading state is already handled by Grafana's built-in `PanelChrome` (a thin loading progress bar, visible once the panel container itself has mounted). The *real* blank window users would perceive (~2.6s, confirmed via a 0ms-delay baseline test giving the same result as a 4000ms-delay test) is Grafana's own plugin-bundle/route bootstrap before this plugin's own React tree exists at all — not something reachable from inside the plugin's own code, since nothing of ours has mounted yet during that window. **If "the page looks broken while loading" comes up again, check whether it's actually this ~2.6s pre-mount window (nothing to fix) vs. a genuinely slow query on an already-mounted page (Grafana's default panel spinner should already be covering that) before writing new loading-state code.**
21. **`SceneObjectBase`'s `_setParent` auto-parents *any* `SceneObjectBase` instance it finds in state, not just `$`-prefixed fields.** A scene object referenced from multiple sibling panels' state (e.g. a shared `SceneQueryRunner` or controller object) gets silently re-parented to whichever consumer constructed last (console warning, easy to miss), breaking its real placement elsewhere in the graph. Keep genuinely-shared references as plain instance fields (custom constructor, outside `SceneObjectState`) or plain closures instead — see the Kubernetes home page's `DetailViewSelection`/`ViewDetailLink` pattern above.
22. **Table panel gotchas found building the Kubernetes home page's tables**: (a) the gradient-bar look is `cellOptions: { type: TableCellDisplayMode.Gauge, mode: BarGaugeDisplayMode.Gradient }`, not `type: TableCellDisplayMode.GradientGauge` (that enum value exists but isn't valid for `TableBarGaugeCellOptions`'s `type`, TS will reject it); (b) the row-count-footer option lives on the **field** (`overrideCustomFieldConfig('footer', { reducers: ['count'] })`), there's no panel-level `options.footer` in this schema version; (c) `PanelBuilders.table().setNoValue('...')` **does** replace the generic "No data" box with custom text for a genuinely zero-row result, not just a null cell in an existing row — confirmed live, useful for "no issues found" empty states.
23. **`Alert`'s `action` prop (`@grafana/ui`) does NOT put the action button next to the message text.** Its internal layout is a `Stack` containing a title `Box` (`flex: 1, minWidth: '50%'`) and a *sibling* `Stack` holding `action` - the title box's `minWidth: '50%'` pushes the action to (at least) the far right half of the banner regardless of how short the actual message is, with no prop to change it. If "the button should sit right after the text" is ever requested again, don't fight `Alert`'s layout - reimplement the banner as a plain flex row using `theme.colors[severity]` tokens (`.main`/`.border`/`.text`/`.transparent` - the same `ThemeRichColor` shape `Alert` itself reads from `theme.colors[severity]`), matching `NamespaceHealthBanner` in `clusterOverviewCards.tsx`.
24. **`InfoCard` (`clusterOverviewCards.tsx`) originally only read `data.series[0]`** - fine when one query backs the whole card, but breaks the moment a second, unrelated query (e.g. a separate EgressIP lookup) is added to the same `$data` without a join/merge transform, since Prometheus's own field-naming convention also shifts from bare `"Value"` to `"Value #<refId>"` once a request has more than one query (caught this exact regression: a `workloads:` row silently went blank after adding the EgressIP query, fixed by both generalizing `InfoCard` to search every frame and updating the row's `fieldName` to the now-disambiguated one). If adding a query to an existing single-query `SceneQueryRunner`, always re-check every consumer's assumed field names.
25. **Elasticsearch's `date_histogram` interval setting has no built-in "auto, but never smaller than X" syntax in this Grafana version** - confirmed directly against a live instance (`curl -X POST /api/ds/query`): `"auto"`, `"$__interval"` (a genuine backend-resolved macro, not just a frontend template-var substitution - verified by sending the literal string straight to the API and getting correctly-sized buckets back), and concrete durations all return 200; `">10s"`/`">1m"` (the Prometheus/Grafana convention for "auto with a floor") and `"$__interval_ms"` both 400. To get "follow the timepicker, but floor at Xm" for an Elasticsearch panel, compute a concrete duration client-side via `@grafana/data`'s `rangeUtil.calculateInterval(timeRange, resolution, lowLimitInterval)` (exported as the `rangeUtil` namespace, not a bare top-level export) and pass the result string directly - there's no way to express the floor in the query itself.
26. **A Bar Chart panel's `overrideColor({ mode: FieldColorModeId.Fixed, fixedColor: 'grey' })` throws at render time** ("Unsupported 'grey' color... nnn/nnnnnn/rgb()/rgba()/hsl()/hsla()/color() only"), while the exact same override with `'red'`/`'orange'`/`'blue'`/`'green'`/`'purple'`/`'dark-red'` etc. all work fine. Grafana's 6 registered viz hues (red/orange/yellow/green/blue/purple, each with `super-light-`/`light-`/`semi-dark-`/`dark-` shade prefixes) get resolved to a real color *before* reaching the Bar Chart's underlying uPlot renderer, but "grey" isn't one of them - uPlot's own color parser gets the literal string and rejects it. Use an actual hex value (e.g. `'#8e8e8e'`) for any "neutral/default/other" bar-chart series color instead of the CSS color name.
27. **`VizPanelBuilder` has no `setTitleItems()`** - `titleItems` (the small elements shown next to a panel's title, e.g. a "panel link" icon distinct from a field/data link) only exists on the built `VizPanel`'s own state. Set it via `const panel = builder.build(); panel.setState({ titleItems: <... /> });` after `.build()`, not through the builder chain.
28. **A `SceneFlexItem`'s `ySizing: 'content'` (row layout) means `alignSelf: 'flex-start'` - it will NOT stretch to match a taller sibling**, even though the row's own auto height is still driven by whichever sibling has the most content. If several cards in a row should visually match height (e.g. an info card + two shorter summary cards), leave `ySizing` unset (default `'fill'` → `alignSelf: 'stretch'`) on the shorter ones - only the item(s) that should *drive* the row's height need `'content'`. The stretched card's own inner content may then need `height: '100%'` + a flex column layout (with e.g. `marginTop: 'auto'` on a trailing line) to make good use of the extra height instead of leaving blank space inside it.
29. **Two metrics that independently derive the same conceptual label (e.g. "workload" for a bare pod) can silently disagree** and it won't surface as a bug until something joins them in a way that requires uniqueness. A plain `OR` union of two disagreeing series is harmless (both rows just appear); `* on (...) group_left(...)` is not - Prometheus requires the join's right-hand side to be unique per match-group, and two rows for the same {cluster,namespace,pod} with different label values hard-fails with `"many-to-many matching not allowed"`. If a `group_left`-joined query that combines a "canonical" attribution source with a raw fallback source (e.g. `namespace_workload_pod:kube_pod_owner:relabel` unioned with `kube_pod_owner{owner_kind=""}`) starts failing, check whether the two sources actually agree on the joined label's value for every entity they both cover - they may already have quietly disagreed for a while in any `OR`-only usage, and even split a `merge`-based table into two incomplete rows without anyone noticing (see the Namespace Drilldown Network/Storage tabs' debug-shell writeup above for a real instance of exactly this).
30. **A "logs"-type Elasticsearch query (`metrics: [{ type: 'logs' }]`, no `bucketAggs`) returns individual documents as a data frame** (fields like `@timestamp`, `message`, plus every other top-level/dot-flattened document field), not aggregated counts - this is what a Grafana `Logs` panel (`PanelBuilders.logs()`) expects, distinct from the `date_histogram`-bucketed queries a Bar Chart/Timeseries panel needs. The message field is auto-detected by common names (`message` worked with zero extra config here); level-based coloring needs the datasource's own `jsonData.logLevelField` set explicitly if the level field isn't literally named `level` (ours is the dotted `log.level`).

## Demo data (`demo/kube-metrics/metrics` + `demo/prometheus/prometheus.yml`)
Has: node info/capacity, per-node CPU/memory recording-rule-style aggregates, namespace-level CPU/memory recording-rule-style sums (**now including the `cluster:namespace:pod_cpu:active:...`/`cluster:namespace:pod_memory:active:...` variants the Namespaces page actually reads, added this session** — see the Namespaces section above), a full set of storage metrics, enriched `ALERTS`, and a small Workloads demo set in namespace `default` on both clusters (Deployment "app", DaemonSet "node-agent", StatefulSet "cache", Job "db-migrate", bare pod "debug-shell").

**This session added the per-pod/per-container inventory metrics the Kubernetes home page's Overview tab needs** (`kube_pod_info`, `kube_pod_container_info`, `kube_pod_status_phase`, `kube_pod_status_condition`, `kube_pod_status_reason`, `kube_pod_container_status_waiting_reason`, `kube_pod_container_status_last_terminated_reason`, `kube_pod_container_status_restarts_total`, `kube_deployment_status_condition`, `kube_node_spec_unschedulable`) — none of these existed anywhere in the file before, so every one of that tab's 12 issue panels and the Clusters/Nodes/Pods/Containers top stats showed "No data". The 10 existing workload pods are modeled healthy; 4 new standalone pods (`canary-rollout-aws` Pending, `netsplit-node-gce` Unknown phase, `evicted-batch-aws` Evicted, `broken-image-gce` ImagePullBackOff) each give exactly one issue panel a real example row. One existing `kube_node_status_condition` row (demo-cluster-aws-node-3's `Ready`/`false`) was flipped from `0` to `1` for the "Nodes not ready" panel — safe to do because `clusterOverviewQueries.ts`'s Cluster Health query only uses that metric's *presence* (`count(...) * 0`) as a fallback, never its value. **"Restarting containers" stays "No data" even with `kube_pod_container_status_restarts_total` populated** — its query uses `increase(...[1h])`, and since this demo file is served statically (nginx, not a real exporter that increments over time), `increase()` over an unchanging counter always evaluates to 0. Same documented limitation as the `container_fs_{reads,writes}_total` counters below, not a bug.

- **No longer true as of this session**: `node_namespace_pod_container:container_cpu_usage_seconds_total:sum_irate`/`:container_memory_working_set_bytes` and `cluster:namespace:pod_cpu:active:.../pod_memory:active:...` **now have real pod+container-level rows** (added to unblock the Efficiency tab, then reused by the CPU/Memory tab work - see below) instead of the old namespace/node-total-only rows. Workloads' CPU/Mem Usage % columns, previously empty "by design" to avoid double-counting into the coarser totals, now show real percentages - the coarser totals (Cluster CPU chart, Namespaces CPU Usage column, `namespace_cpu:.../namespace_memory:...`) were **left alone** (still their old, disconnected-from-the-per-container-sum values) to avoid touching the already-shipped, already-verified Cluster Overview/Namespace Overview charts, so a *slight* numeric inconsistency between "coarse total" and "sum of the now-real per-container rows" is expected and was a deliberate scope-containment call, not an oversight.
- `container_fs_{reads,writes}_{bytes_,}total` and (new this session) `container_network_{receive,transmit}_{bytes_,packets_dropped_}total` counters are static, so `rate()` on them evaluates to 0 — Storage tab Throughput/IOPS and Network tab Bandwidth/Saturation panels (both Cluster- and Namespace-level) showing flat 0 is expected, not a bug.
- **The demo Prometheus only has a shallow, discontinuous data window** (recreated across sessions) — day/week-before comparisons on the per-panel compare feature won't show an overlay line until it's been running continuously that long.
- **Before assuming a page's empty column is a demo-data gap vs. a code bug**: check whether the underlying metric name genuinely doesn't exist anywhere in `demo/kube-metrics/metrics` (`curl .../api/v1/query --data-urlencode "query=<metric>"` against the live Prometheus) *and* whether a differently-named-but-equivalent recording rule already exists for a different page (as happened with the Namespaces requests/limits metrics this session) before concluding it's "just missing data" — it might be a genuine naming/wiring bug instead.

## Demo stack
Bind-mounted to this worktree's `dist/`/`demo/` paths as of this session (recreated after edits to `demo/kube-metrics/metrics`, per gotcha #6). `node_modules` here is a real, independent `npm ci` install, not the shared symlink (gotcha #7). `debeka-k8s-app` also has the org's patched `grafana-pi-app` build mounted over the official Assistant path, sourced from a *different* session's scratchpad path — ephemeral, don't assume it's still there in a future session; check with `podman inspect`.

**This session's sandbox had no real user login session** (`/run/user/1000` doesn't exist and can't be created — `mkdir` → Permission denied; `systemctl --user`/`loginctl` → "Failed to connect to bus"; `sudo` needs a password with no TTY) — rootless `podman`/`podman-compose` needed manual setup before either compose file would run at all:
1. `~/.config/containers/storage.conf`: `runroot`/`graphroot` pointed at writable `/tmp` paths (`overlay` driver).
2. `~/.config/containers/containers.conf`: `[engine] tmp_dir = "/tmp/podman/tmp"`, `cgroup_manager = "cgroupfs"`.
3. `XDG_RUNTIME_DIR=/tmp/podman/xdg` exported **inline in every single command** (config files alone aren't enough — crun/aardvark still fall back to `/run/user/1000` without it — and it must be inline since env vars don't persist between separate tool-call shells, and non-interactive shells skip `~/.bashrc`). Needs `mkdir -p "$XDG_RUNTIME_DIR/libpod/tmp" "$XDG_RUNTIME_DIR/containers"` once.

Even with all three fixed, two more sandbox-specific issues showed up:
- **`aardvark-dns` (the per-network container-name resolver) never actually starts** — `podman network inspect` claims `dns_enabled: true` with a gateway IP, but nothing listens on `<gateway>:53`, so any container resolving another by hostname (Grafana → `prometheus`/`elasticsearch`/`rqlite`; Prometheus → `node-exporter-aws/gce`/`kube-metrics-demo`) times out. **This is what "I get No data everywhere" turned out to mean** — Prometheus itself had scraped nothing. Fix: look up each container's real IP (`podman inspect <name> --format '{{(index .NetworkSettings.Networks "debeka-k8s-app-demo").IPAddress}}'` — note the `index` call, a hyphenated network name breaks plain dot access in Go templates) and add static `/etc/hosts` entries with `podman exec -u root <container> sh -c "..."` in *every* container that needs to resolve another one, not just Grafana. `sed -i` fails on `/etc/hosts` ("Device or resource busy", it's a bind-mounted special file) — overwrite it by writing full new content and `cat`-ing it back in place instead of renaming.
- **Bind mounts go stale on host-file edits, even from a correct source path.** Editing `demo/kube-metrics/metrics` after `kube-metrics-demo` (nginx) was already running left the container's view frozen at creation-time content (different inode/size/mtime from the host file) — `podman restart` doesn't fix it, only `podman rm -f` + recreate does. A fresh container gets a new IP, so re-check/re-patch any other container's `/etc/hosts` entry pointing at the old one. **Generalizes gotcha #6 below** (stale *worktree path*) to stale *content on the right path* — recreate after editing anything a running container bind-mounts, don't just wait or restart.

Full recipe, in order: `podman-compose -f docker-compose.demo.yaml up -d` → build+run Grafana (`podman-compose -f docker-compose.yaml up -d --build` gets the image built, but **its `../dist`/`../provisioning` volume paths resolve one directory level too high** — `docker-compose.yaml`'s `extends: .config/docker-compose-base.yaml` bug, confirmed via mismatched `podman inspect --format '{{range .Mounts}}...'` source paths; `build.context: .` in the same base file correctly resolves relative to `.config/`, only the `volumes:` paths are affected — so `podman rm -f debeka-k8s-app` and re-run with a **direct `podman run`** using explicit absolute paths and `--net debeka-k8s-app-demo` instead) → patch `/etc/hosts` in Grafana and Prometheus → verify via `curl localhost:3000/api/datasources/uid/<uid>/health` and `curl localhost:9090/api/v1/targets`.

## Where to pick up next
- **The Namespace, Workload, AND Pod Drilldowns are now all fully built out - all 7 tabs each (Overview, CPU, Memory, Network, Storage, Logs, Events) have real content.** No more placeholder scaffolds on any of the three.
- **The Node Drilldown (new this session) only has its Overview tab built out** - CPU/Memory/Network/Storage/Logs/Events are still `getNodePlaceholderScene` scaffolds (same shape as the other drilldowns' own placeholders before they were filled in). This is the most likely "next tab" ask - the user built this one incrementally (Overview shell first, then info-card content, then optimization+Pods table, each its own message), so expect the same pattern to continue rather than a single "build the rest" request.
- **`v1.10.0` was released this session** (see the top-of-file note and the "Session 2026-08-19" section) - the next batch of work should open a fresh `## 1.10.1` (or higher) heading in `CHANGELOG.md`.
- **The Node Drilldown's VCF/vSphere info card (`vcf_vcenter`/`vcf_clustername`/`vcf_esx_host`) is unverified against real data** - the local demo stack has no vSphere/telegraf metric source at all, so it's only been confirmed to not error (shows "–" everywhere). The query is a chained PromQL self-join against `vsphere_vm_cpu_idle_summation` translating a 3-step "vmname→esxhostname→clustername→vcenter" label lookup the user described verbally - if it doesn't resolve correctly against a real environment, the most likely issue is that metric's actual label set differing from what was described (e.g. if `clustername`/`vcenter` are already present directly on the per-VM series, the self-join is unnecessary complexity; if the join keys don't line up 1:1, the `group_left` steps could silently produce no match or "many-to-many" errors - see gotcha #29 in this same file for a precedent of exactly that failure mode elsewhere).
- **EgressIP resolution was fully rebuilt this session** - the OpenShift cluster-name-parsing + application/substage RQLite indirection from the prior session (`v1.9.1`) is gone entirely, replaced by a single direct RQLite query keyed by namespace. If EgressIP still doesn't work in prod after `v1.9.3`, the next thing to check is the `g42-rqlite-datasource` plugin's actual runtime behavior for the confirmed `rawSql`/`editorMode: 'code'` query shape (not the shape itself, which was confirmed against a real panel) - see the "Session 2026-08-18" section's EgressIP write-up for the full diagnostic trail (`useSceneApp` caching, `DataSourceVariable` first-match fallback, Mixed-datasource field-naming) in case a *different* symptom comes back.
- **Efficiency tab's two "waste by namespace (top 10)" panels are `PanelBuilders.bargauge()` by inference, not confirmed against a populated reference** — Grafana Play's own instance showed "No data" for both when inspected, so there was nothing to visually copy. If the user has a real cluster with actual waste data, worth re-checking against Grafana Play again and adjusting the viz type if it turns out to be something else (bar chart, table, etc.).
- **Deployed container images' "Total" footer doesn't render** in this environment despite correct schema usage — see the Kubernetes home page section above. Worth a closer look (maybe a different Grafana version, maybe TableNG vs. the classic table renderer behave differently) rather than assuming it's fixed.
- **Namespace Drilldown's PVC Volume Bytes/inodes panels' `ALERTS{alertname="KubePersistentVolumeFillingUp"/"KubePersistentVolumeInodesFillingUp"}` series have no matching demo alert firing** — those two panels' 5th/4th series (respectively) will show no line for them; harmless (the other Requests/Capacity/Used/Total/Used lines still render), but worth adding a firing demo alert for one of these if asked to make that panel's alert-marker series visibly demonstrable.
- The multi-cluster pod-name-collision risk in Workloads' cpu/mem queries (`on(namespace, pod)` without `cluster`) is a real, not-yet-fixed characteristic of the given queries.
- `grafana-pi-app` PR #32 is merged; nothing pending there unless the user reports a *new* Assistant-integration symptom.
- Network/Storage tabs (Clusters Drilldown) have no variable pickers (queries only reference `$cluster`) — confirm before assuming this is wanted if asked to extend them. The new Namespace Drilldown CPU/Memory/Storage tabs *do* have a Pod picker (Network doesn't - no query there references `$pod`).
- `clustersApp.tsx` (the second-largest file, ~1800 lines) was flagged by graphify as an optimization candidate but not split — a natural next pass would be one file per tab (Overview/CPU/Memory/Network/Storage), mirroring the `resourceSimulatorFormatters.ts` split **and this session's own `namespacesPage.tsx` split** (CPU/Memory/Network/Storage each went into their own `namespace<Tab>Scene.tsx` + `namespace<Tab>Queries.ts` file pair instead of growing the already-large `namespacesPage.tsx` further - `namespacesPage.tsx` itself now only owns the list page, the drilldown shell/tab-routing, the Overview tab, and the Logs/Events tabs).
- If asked to add more timeseries panels to Clusters/Namespace Drilldown or Alerts, remember to chain `.setHeaderActions(new PanelTimeRangeCompare())` before `.build()` so they pick up the comparison control automatically.
- The Namespaces-style combined meter cells now cover Namespaces, Workloads, and the Namespace Drilldown's own Overview/CPU/Memory Workloads tables - if a *new* page needs the same treatment, reuse `attachPercentField`/`requestUsageCell`/`usageTierCell` from `tableCells.tsx` directly rather than reinventing them, and reuse gotcha #19's `excludeByName`-not-`hideFrom` pattern.
- **Every demo-stack restart/recreate this session needed the full `/etc/hosts` re-patch dance again** (gotcha in the Demo stack section) - `podman restart` on Grafana or Prometheus, or `podman rm -f` + recreate on `kube-metrics-demo`, all reset `/etc/hosts` and/or hand out a new container IP. When several of this session's `demo/kube-metrics/metrics` edits needed to land at once (common when iterating on one tab's demo data), it was faster to batch them into one file edit, then do **one** `kube-metrics-demo` recreate + hosts re-patch, rather than repeating the dance per edit.

## Session 2026-08-17: Namespace Drilldown filter changes + new "All Jobs" page (UNCOMMITTED)

**Namespace Drilldown Storage tab** (`namespaceStorageScene.tsx`/`namespaceStorageQueries.ts`): Throughput/Throughput by workload/IOPS/IOPS by workload legends moved `placement: 'right'` → `'bottom'` (kept `LegendDisplayMode.List`/`calcs: ['lastNotNull']` - only placement changed, per explicit user ask, not a broader legend-convention change). **Pod filter removed entirely** from this tab (picker, `$variables`, and every `pod=~"$pod"` matcher stripped from the queries, including the `workloadAttributionWithPodFragment` helper, renamed to `workloadAttributionFragment` since it's now pod-less like the Network tab's own version) - confirmed via clarifying question that the user's mention of a "global namespace filter" here was a mistake (see below), not an actual ask.

**CPU tab / Memory tab**: added a **local Workload filter**, additive alongside the existing Pod filter (both `VariableValueControl`s now sit side by side). `createWorkloadFilterVariable` in `datasourceVariables.ts` was **extended** (not duplicated) with optional `clusterRegex`/`namespaceRegex` overrides - same "literal override for pages without a scene-level variable to reference" pattern `createNamespaceFilterVariable` already had; the Workloads list page's own call site is unaffected (falls back to the scene-level `${cluster:regex}`/`${namespace:regex}` defaults). The Distribution/Alignment panels' `workload=~".+"` matcher (previously a fixed "any real workload" filter with no picker) became `workload=~"$workload"`; the Workloads table (no PromQL workload filter available) is row-filtered client-side via `filterByValue`, same pattern as `workloadsPage.tsx`'s own Workload filter.

**Clarifying question asked and answered**: the user's initial ask mentioned a "global namespace Filter" for the Storage tab, which didn't fit the page's architecture (namespace is already fixed via the drilldown route, not a picker). Asked via `AskUserQuestion` rather than guessing; the user confirmed it was a mistake - they meant only "remove the Pod filter" for Storage, no namespace filter at all. **Worth remembering**: when a request's wording contradicts the concrete architecture you just read, ask rather than build the wrong picker on a real production Scenes page - the two candidate readings here (a genuine multi-namespace comparison picker vs. a simple slip of the tongue) would have meant very different amounts of work.

**New "All Jobs" top-level page** (`src/pages/Jobs/jobsPage.tsx` + `src/queries/jobsQueries.ts`), registered in `constants.ts` (`ROUTES.Jobs = 'jobs'`), `plugin.json` (`includes` entry between Workloads and Nodes, `addToNav: true`), and `clustersApp.tsx`'s `SceneApp.pages`. Kubernetes-icon + "All Jobs" title, Datasource/Cluster/Namespace page-level filters (`createClusterFilterVariable`/`createNamespaceFilterVariable`, same pattern as the Kubernetes home page), two tabs:
- **Cronjobs tab**: one table, 5 queries (`exists`/`last_success`/`last_schedule`/`next_schedule`/`status`) merged on their common `(cluster, namespace, cronjob, join_name)` fields (`exists` alone also carries `schedule`, same "merge only needs *some* shared fields, not identical field sets across all frames" reasoning as every other multi-query table in this codebase). STATUS (`kube_cronjob_spec_suspend`, 0/1) uses a `ValueToText` mapping + `ColorBackground` cellOptions (0→green "Active", 1→orange "Suspended") - same mechanism as `alertsPage.ts`'s own `severityMappings`, not a bespoke cell component. LAST SUCCEEDED/LAST SCHEDULE/NEXT SCHEDULE use unit `dateTimeFromNow`.
- **Jobs tab**: one table, 4 queries (`start`/`end_time`/`success`/`completion`), merged similarly (`success` alone carries `owner_name`/`owner_kind` → CONTROLLED-BY/TYPE). DURATION is a `calculateField` transform (`mode: 'binary'`, `binary: {left: 'Value #end_time', operator: '-', right: 'Value #start'}`, `alias: 'DURATION'`) - passing plain field-name strings for `left`/`right` works, `@grafana/data`'s `checkBinaryValueType` auto-wraps a non-numeric string into a `{matcher: {id: byName, options: ...}}` field reference; unit `dtdurationms` on the resulting field. PODS/COMPLETION **reuses `attachDesiredPodsField`/`readyDesiredPodsCell` from `tableCells.tsx` directly** (the existing "ready / desired" bar+text cell, same shape as the Workloads page's own Pods column) rather than a new renderer.

**Given queries had no `$cluster`/`$namespace` variable tokens at all** (every matcher hardcoded `cluster=~".+"`/`namespace=~".+"`, unlike every other tab's Grafana-Play-sourced queries) - since the page explicitly needed working Cluster/Namespace filters, `substituteJobsClusterNamespace()` in `jobsQueries.ts` does a literal-substring replace of exactly those two matcher forms (not a blanket `".+"` replace - e.g. `owner_name=~".+"` and branches with no `cluster=~` matcher at all are left untouched, same "translate literally, preserve intentional omissions" convention as every other tab).

**New gotcha #31: Prometheus normalizes a label with an empty string value (`owner_kind=""`) as equivalent to the label not existing at all** - confirmed live: `kube_job_owner{...,owner_kind="",owner_name=""}` demo rows for a bare/standalone Job produce a `success` query result with **no `owner_kind`/`owner_name` field at all** (not a field with empty values - the field is entirely absent from the frame), so the Jobs table's CONTROLLED-BY/TYPE columns don't even render for that row. This matches real kube-state-metrics behavior too (not a demo-data artifact) - a genuinely standalone Job's CONTROLLED-BY/TYPE will always be blank in a real cluster for the same reason. Verified the columns DO render correctly by temporarily giving `infra-check-29160480`/`user-check-29160480` (which already have non-empty `owner_kind="CronJob"`/`owner_name` in the pre-existing Cluster Health demo data) matching `kube_job_status_succeeded`/`kube_job_spec_completions` rows - confirmed via screenshot, then reverted (kept the demo data minimal/clean rather than leaving in a row with `Duration: NaN`, since those two jobs' pre-existing `kube_job_status_start_time` is a stale fixed 2025 timestamp the Jobs tab's own `> (time() - $__range)` filtering excludes).

**Demo data added** (`demo/kube-metrics/metrics`, additive only): `kube_cronjob_info`/`_status_last_successful_time`/`_status_last_schedule_time`/`_next_schedule_time`/`_spec_suspend` for the existing `infra-check`/`user-check` CronJobs (reused rather than inventing new entities - zero risk to the Cluster Health feature those two already back); `kube_job_owner`/`_status_start_time`/`_status_completion_time` added for the existing `db-migrate` Job (bare/standalone, `owner_kind`/`owner_name` left empty on purpose - see gotcha #31). **All the new relative timestamps are anchored to when this file was edited (~2026-08-17 11:00 UTC), not live** - same static-file limitation as the pre-existing `kube_job_status_start_time` values elsewhere in this file; the Cronjobs tab's own timestamp fields aren't time-range-filtered so they'll just show an ever-growing "X days ago" as this drifts, but the Jobs tab's START/END *are* gated by `$__range`, so `db-migrate`'s row will silently drop out of the default 1h view after roughly 45–60 minutes from whenever this was written, until refreshed with new relative timestamps.

**Verified in-browser** (Playwright, headless Chromium via the extracted-system-libs trick from gotcha #18 - the libs weren't present this session, re-extracted to `/tmp/claude-*/.../scratchpad/pwdeps/extracted`, not preserved anywhere durable): screenshotted Storage/CPU/Memory tabs (legend placement, Pod-filter absence on Storage, Pod+Workload filters side by side on CPU/Memory, Workloads tables) and both All Jobs tabs (Cronjobs table with colored STATUS badges and populated schedule/timestamp columns; Jobs table with populated JOB/CLUSTER/NAMESPACE/START/END/DURATION/PODS-COMPLETION for `db-migrate`, and CONTROLLED-BY/TYPE separately confirmed populated for CronJob-owned jobs before reverting that demo addition). `tsc --noEmit` and `eslint` both clean on every changed/new file. Committed and released as part of `v1.9.0` - see the top-of-file note.

**Immediate follow-up correction (same session)**: the user actually wanted the CPU/Memory tabs' **Pod filter removed too** (not kept alongside Workload) - only the Storage tab's Pod-filter removal had been explicit in the original ask; CPU/Memory initially kept Pod+Workload side by side. Fixed: `POD_VARIABLE_NAME`/`createPodFilterVariable` fully removed from both `namespaceCpuScene.tsx`/`namespaceMemoryScene.tsx` (imports, `$variables`, control, and every `pod=~"$pod"` matcher stripped from `namespaceCpuQueries.ts`/`namespaceMemoryQueries.ts`, same mechanical strip as the Storage tab's earlier removal) - both tabs' `substitute()` now call `substituteClusterAndNamespace` directly instead of `substituteClusterNamespacePod`. The Workload `VariableValueControl` was also **moved above the stat-panel row** (was in its own row below the stats, alongside Pod) - now the first item in the tab's body, directly above "Alerts: Firing".

**Gotcha reconfirmed**: `podman restart` on the Grafana container **wipes the hand-patched `/etc/hosts`** (reverts to just its own self-entry) even though it doesn't get a new IP or need a `podman rm -f` recreate - the demo-stack `/etc/hosts` dance from gotcha #6/the Demo stack section applies to `restart`, not just `rm -f`+recreate. Caught this because the rebuilt-and-restarted CPU/Memory tabs briefly showed every panel with a "no data" glyph - `curl .../api/datasources/uid/thanos-demo/health` immediately showed the real cause (`dial tcp: lookup prometheus ... connection refused`), not a code bug.

**Fourth follow-up (same session): added the "Job status: complete/running/failed" legend above the Jobs table** - a `JobStatusLegend` component in `jobsPage.tsx`, reusing the exact same right-aligned "spacer + SceneReactObject legend" row layout as the Namespaces list page's own `ResourceUsageLegend` (`namespacesPage.tsx`), just with green/orange/red job-status labels instead of usage-tier icons. Cronjobs tab's STATUS column (Active/Suspended) intentionally has no matching legend - not asked for, and self-explanatory as plain text.

**Third follow-up (same session): All Jobs table styling + drilldown links, matching Grafana Play reference screenshots.** Cronjobs' STATUS column switched `TableCellDisplayMode.ColorBackground` → `ColorText` (plain colored text, no filled pill - the mapping's `color` field still drives it either way, only the cell rendering mode changed). Jobs' PODS/COMPLETION switched from the Workloads-style ready/desired bar+text cell to a bespoke `jobPodsCompletionCell()` (plain "success / completion" text colored by **job status** - green=complete, orange=running, red=failed - not the old ready-vs-desired 2-color scale): required a new `kube_job_status_failed`-based `failed` query in `jobsQueries.ts` (same shape as `success`, not part of the originally given query set - added since the given queries had no failure signal at all) and `attachFieldValues` (previously module-private in `tableCells.tsx`) exported so the page could stash both `completion` and `failed` counts onto the `success` field's own config with two separate keys. **Real validation, not staged**: `infra-check-29160480`'s pre-existing `kube_job_status_failed{...}=1` demo row (from an earlier session's Cluster Health data, unrelated to this session's additions) made that exact row render red while its sibling rows render green - confirms the status derivation works on genuinely independent data, not just the deliberately-added demo rows.

Both tables' CLUSTER/NAMESPACE columns got `overrideLinks` - `${CLUSTERS_URL}/${__value.text}${__url.params}` (Clusters Drilldown) and `${NAMESPACES_URL}/${__data.fields.cluster}/${__value.text}${__url.params}` (Namespaces Drilldown) - same native-table-link idiom as `namespacesPage.tsx`'s own Cluster/Namespace columns (the `${__data.fields.cluster}` macro reads the *raw* `field.name`, unaffected by `organize`'s `renameByName`-only-touches-displayName behavior per gotcha #1 - confirmed still works even though the column header now reads "CLUSTER").

**Second follow-up (same session): "Overview: Usage (vCPU cores)"/"Overview: Usage (memory bytes)" didn't respect the Workload filter.** Root cause: `namespaceCpuOverviewUsageQueries`/`namespaceMemoryOverviewUsageQueries` (as given) have no workload dimension at all - a flat `sum by () (max by (cluster, namespace, pod, container) (...))` over every container in the namespace - unlike Distribution/Alignment, which already join against `namespace_workload_pod:kube_pod_owner:relabel`. Fixed by wrapping each of the three (limits/requests/usage) in the same existence-join idiom Distribution already uses (`sum by (cluster, namespace, pod) (...) * on (cluster,namespace,pod) group_left() group by (cluster, namespace, pod) (namespace_workload_pod:kube_pod_owner:relabel{cluster="$cluster", namespace="$namespace", workload=~"$workload"})` - the `group by` on the right-hand side collapses every matching series' value to 1, so the multiply is purely a pod-existence filter, not a scale). Verified via `?var-workload=app`/`?var-workload=cache` in the URL: Overview's requests/usage numbers now narrow to just that workload instead of staying at the full-namespace total. The 3 stat tiles (Alerts: Firing/Scheduling/Alignment) were deliberately left alone - not part of this ask, and no complaint about them.

## Session 2026-08-17 continued: v1.9.1 fix batch ("Direkte ein paar Fixes")

Four small, independent fixes requested together, released as `v1.9.1` (separate release from `v1.9.0` earlier the same session - the user asked for the two releases separately, at different points).

**Workloads table Namespace column had no link** (`workloadsPage.tsx`) - Cluster already did (`overrideLinks` → Clusters Drilldown), Namespace didn't. Added the same `${NAMESPACES_URL}/${__data.fields.cluster}/${__value.text}${__url.params}` link the Namespaces list/All Jobs tables already use. New `NAMESPACES_URL` const in this file (didn't exist there before, only `CLUSTERS_URL`).

**Cluster Drilldown "Provider" field** (`clusterQueries.ts`'s `clusterTableQueries.info`, read by the info card in `clustersApp.tsx`) - was `provider_id` (`kube_node_info`'s standard kube-state-metrics label, a full `<scheme>://<node-id>` URI like `vsphere://4231a2c1-...`) run through a `label_replace` regex to strip everything after `://`. The user explicitly wants a **dedicated `provider` label on the metric** used directly instead - swapped `provider_id`→`provider` throughout the query (3 `by(...)` clauses) and dropped the now-unneeded `label_replace`. **This demo's `kube_node_info` only had `provider_id`, not `provider`** - added `provider="aws"`/`"gce"` to the 5 existing node rows (additive, `provider_id` kept too) so the demo doesn't regress to blank. Two other `provider_id` references in `clustersApp.tsx` (the Clusters-list-page `excludeByName` and the Cluster Drilldown's own Nodes-table `excludeByName`, both just hiding it from a table, not displaying it) - only the list-page one reads from the *same* `clusterTableQueries.info` query and needed the same rename; the Nodes-table one reads a *different* query (`clusterNodeTableQueries.info` in `clusterOverviewQueries.ts`, still `provider_id`-based) and was deliberately left alone - not displayed anywhere, no user complaint, out of scope.

**Gotcha #32: `last_over_time(expr[$__range:])` does NOT respect Prometheus staleness markers within its lookback window**, unlike a plain instant-vector selector. After recreating `kube-metrics-demo` with the new `provider` label, the *old* label-set's series (without `provider`) kept showing up in `last_over_time(...[1h:])`-wrapped queries (like `clusterTableQueries.info`) for the **entire default 1h dashboard window** - not just ~5 minutes as gotcha #6/staleness-delta intuition would suggest. Confirmed via direct `/api/v1/query` calls: a short lookback (`[30s:]`) showed only the correct new series; the full `[1h:]` one kept showing both (old value blank, new value correct) for 20+ minutes after the recreate. **If "the panel shows a stale/blank value right after a demo-data label-set change" comes up again, this is why** - it's not actually a code bug, just wait out the dashboard's own time range (or shrink it temporarily) before concluding something's broken. Real users won't hit this since they don't recreate the scrape target mid-session.

**Namespace Drilldown CPU/Memory quota cards** (`NamespaceQuotaCard` in `namespaceOverviewCards.tsx`) - previously the *whole card* was a `role="button"` `onClick`/`onKeyDown` navigating to the Resource Simulator (pre-filtered to cluster+namespace, that part was already correct). Replaced with the same **Panel Link (title-bar icon) + Value Link (on the number)** pattern already established for the Kubernetes home page's stat tiles (`PanelLinkTitleItem` from `panelLinks.tsx`, reused directly - it's a plain React component, not tied to `VizPanel` internals, so it drops into this hand-rolled card's own header row fine). Two things worth remembering if this pattern gets reused again: (a) `summaryHeader`'s CSS is `justifyContent: 'space-between'` - adding a 3rd direct child (the link icon, after label+badge) floats the badge into the middle instead of next to the icon; fixed by wrapping badge+icon in their own inner flex row so `space-between` still only sees 2 top-level children; (b) the value was a bare `<strong>` (bold via the tag itself, no CSS class does it) - swapping the tag to `<a>` for the Value Link silently drops that bold weight unless a `<strong>` is nested back inside the `<a>`.

**EgressIP `environment_debeka_de` resolution via RQLite** (`namespaceOverviewQueries.ts` + `namespacesPage.tsx`) - previously assumed `environment_debeka_de == namespace` always; the user clarified it's actually `<application>-<substage>` (application coordinates) which only *sometimes* matches the namespace name. New `parseOpenshiftClusterCoordinates(cluster)` extracts `dc_area`/`dc_tenant`/`dc_cluster` from the cluster name via the three given regexes (real prod names follow `openshift-<dc_cluster>-<dc_tenant>-<dc_area>`; this demo's `demo-cluster-aws`/`-gce` names don't, so parsing returns `undefined` there and the code **falls back to the namespace name directly - the old behavior**, exactly what the demo still exercises, no regression). When it *does* parse, two **hidden** `QueryVariable`s (`namespaceApplication`/`namespaceSubstage`, `hide: VariableHide.hideVariable`) run the user's two given SQL queries against the RQLite datasource (`infraDatasource()` from `queries/datasources.ts`, `${infraDatasource}` token - needed its own hidden `createRqliteDatasourceVariable()` added to this tab's `$variables` too, since the Namespace Drilldown page didn't have one at any scope before), and the EgressIP PromQL's `environment_debeka_de` becomes `${namespaceApplication}-${namespaceSubstage}` (real Grafana variable interpolation, resolved at query-run time - not JS string substitution, since these two values only exist after their own queries run).

**Not verified end-to-end: the `g42-rqlite-datasource` plugin isn't installed in this sandbox's Grafana** (`curl .../api/ds/query` against `rqlite-demo` → `"Plugin not registered"`) - couldn't confirm the exact query JSON shape the plugin's `metricFindQuery` expects. Used `QueryVariable`'s `query` field as a **plain string** (the raw SQL text directly, no wrapping object) - this is the standard/most broadly-compatible shape for Grafana template-variable queries (works via `metricFindQuery(rawQueryString)`, distinct from a panel's full `DataQuery` object with a `rawSql` field), and `QueryVariableState.query` is typed `string | SceneDataQuery` in `@grafana/scenes`, confirming a bare string is a legitimate option - but this specific plugin's actual behavior is unconfirmed. **Told the user explicitly to verify this against their real instance** - if it turns out wrong, the fix is almost certainly changing `query: buildNamespaceApplicationQuery(...)` (a string) to `query: { refId: 'application', rawSql: buildNamespaceApplicationQuery(...) }` (a `SceneDataQuery`-shaped object) or whatever field name the plugin's source actually expects.

## Session 2026-08-18: Workloads Drilldown page (v1.9.2–v1.9.4)

Built the entire Workloads Drilldown page from scratch across the whole session, then fixed three follow-up issues the user found live. Started by reading this file (per `CLAUDE.md`'s "read summary.md" instruction) and `git log --oneline -10`/`git worktree list` (per the parallel-agent note above) before doing anything.

### Demo stack setup (needed to verify each tab as it was built)
Reapplied the established rootless-podman recipe from the "Demo stack" section above verbatim - still needed, sandbox is fresh each session. Two **new** findings this time, worth remembering alongside the existing ones:
- **This worktree's `node_modules` was missing `@grafana/assistant` entirely** (declared in `package.json`, but not actually installed on disk - `tsc`/`webpack` both failed with "module doesn't exist"), even though the previous session's `npm ci` should have covered it. Fixed with a targeted `npm install @grafana/assistant@^0.1.33` rather than a full reinstall. **Watch out**: this bumped `package-lock.json`'s resolved version to `0.1.34` (still satisfies the `^0.1.33` range, but an unintended, unrequested drift) - reverted `package-lock.json` before committing each release this session, keeping only the intentional `npm version` bumps. If a future session's `npm install`/`npm ci` similarly nudges a lockfile version, check `git diff package-lock.json` before committing and revert anything not deliberately asked for.
- **`elasticsearch-seed`/`rqlite-seed` (the one-shot Python seeding containers) get stuck looping forever in this sandbox** - they resolve `elasticsearch`/`rqlite` by container hostname (`ES_URL`/`RQLITE_URL` env vars), and aardvark-dns doesn't work here (gotcha in the "Demo stack" section above), so their own internal "wait for datasource to respond" retry loop never succeeds and the container just sits "Up" forever without ever actually seeding anything. **This is easy to miss** - the container *looks* healthy (`Up X minutes`), and doesn't error, so nothing suggests seeding failed until a query for freshly-added fields comes back empty. Fix: run the seed script **directly on the host** against the datasource's own published port instead of relying on the container (`ES_URL=http://localhost:9200 python3 demo/seed/elasticsearch_seed.py`), then `podman rm -f` the stuck container so it's not left silently spinning. Re-seeding is safe/additive (the script's own `_mapping` PUT is idempotent, and it just appends more documents), but only fixes documents indexed via that direct run - anything the container *would* have seeded stays missing until re-run this way.

### Building the page, tab by tab
The user pasted PromQL/Lucene queries per-tab across several turns (Overview → CPU → Memory → Network → Storage → Logs/Events), same "translate literally" workflow as every other drilldown page - each turn's queries went into their own `workload<Tab>Queries.ts` file, wired up in a matching `workload<Tab>Scene.tsx`, both under `src/pages/Workloads/`.

**Route structure**: `/workloads/:cluster/:namespace/:workloadType/:workload/*` - four params, since a workload's real identity is `(cluster, namespace, workload, workload_type)` and workload names aren't unique even within one namespace across different owner kinds (the Workloads list page already established this via `merge`, not `joinByField`).

**The defining pattern that makes this page simpler than the Workloads list table it's reached from**: since the exact workload identity is already known from the route, no query needs the multi-branch bare-pod/static-pod/replicaset/attributed-workload fallback chain the *list* page's queries need to cover every possible workload at once. Ready/desired replicas, creation timestamp, and every Network/Storage "by pod" query just select the workload's own kind-specific metric directly (`buildWorkloadReadyDesiredQueries`/`buildWorkloadCreatedQuery` in `workloadOverviewQueries.ts`, dispatching on `workloadType`).

**Hidden Pod variable, not a picker**: introduced in the Overview tab, then repeated per-tab (CPU/Memory/Network/Storage each construct their own, since each tab is its own `EmbeddedScene` with no shared `$variables` across tabs). `createPodFilterVariable` (`datasourceVariables.ts`) gained an optional `workload` param this session - when given, switches its underlying `label_values(...)` query from `kube_pod_info` (no workload label) to `namespace_workload_pod:kube_pod_owner:relabel{...,workload="X"}` (has one). Always `.setState({ hide: VariableHide.hideVariable })` - there's nothing left on this page for a user to further filter by pod, it's pure plumbing for queries that only know how to filter by pod.

**Two shared UI mechanisms extracted out of `namespacesPage.tsx` into new files** so both the Namespace and Workload Drilldowns' Overview tabs (and, for the second one, their dedicated Logs/Events tabs) share one implementation instead of copy-pasting: `src/scenes/logsEventsLevelToggle.tsx` (`LogsEventsLevelToggle`, the Overview tab's per-level bar-chart toggle - generalized to take `buildLogsQueries`/`buildEventsQueries` callbacks instead of a hardcoded cluster/namespace pair) and `src/scenes/logPanels.tsx` (`LogsTabLevelToggle`/`buildLogPanel`, the dedicated tabs' raw-log-panel toggle - these were already fully generic, just not yet shared).

**`InfoCard` generalized** (`clusterOverviewCards.tsx`): added an optional per-row `render(frames: DataFrame[]) => string` callback alongside the existing `fieldName` lookup, for a value that's already known outright (cluster/namespace, taken straight from the drilldown's own route params - no query needed) or that combines several fields (ready/desired replicas as one "1 / 1" string). `fieldName` became optional as a result. `findFieldAcrossFrames` was extracted as its own export so `render` callbacks can reuse the same multi-frame search the default `fieldName` path already used.

**Column-naming/scope corrections made along the way** (worth remembering if a similar page gets built again): the Overview tab's Pods table combines CPU/Memory Requests with their "%" columns into one `requestUsageCell`, matching the Namespaces/Workloads list-table convention, but the CPU/Memory *tabs'* own "Pods" tables (a **separate**, later ask, with an explicitly-given 5-column list) do **not** - the "combine requests/percent" instruction only applied to "the table" from the immediately-preceding turn, not retroactively to every table on the page. When a request says "the table" after several tables were built across multiple turns, check which one it's actually referring to rather than assuming "all of them."

**One apparent copy-paste typo fixed, not preserved**: the Storage tab's given "IOPS by pod" Tx query reused `container_fs_reads_total` instead of `container_fs_writes_total` (its own Rx line, and every other Tx line across this whole session's Storage/Network queries, correctly used the write-side metric). Unlike an `=`-vs-`=~` operator asymmetry (plausibly intentional, always preserved per this project's convention), reproducing this verbatim would make the "Writes" series just duplicate "Reads" - no plausible reason to want that, so fixed and called out explicitly in the CHANGELOG/summary rather than silently "fixing" *or* silently reproducing a likely bug.

**New demo data added**: `kube_deployment_created`/`kube_daemonset_created`/`kube_statefulset_created`/`kube_job_created`/`kube_pod_created` (none existed anywhere before - needed for the Overview tab's "create date" row) for the same app/node-agent/cache/db-migrate/debug-shell entities the rest of the demo already models. Elasticsearch seed data (`demo/seed/elasticsearch_seed.py`) extended with `orchestrator.namespace`/`orchestrator.resource.name` fields (mapped `keyword`, same reasoning as gotcha #30) on the existing ECS/OpenShift-shaped log/event documents, reusing the same pod names (`app-aws`, `node-agent-0-aws`, etc.) the Prometheus side already models, so a workload's logs/events line up with its own metrics.

### EgressIP: reported broken in prod, fully rebuilt

The user reported EgressIP "still doesn't work" on their real instance after `v1.9.1`'s RQLite-based resolution, suspecting a datasource-configuration problem. Investigated by reading actual `@grafana/scenes` source (not guessing):

- **New gotcha #33: `useSceneApp(factory)` caches its result in a module-level `Map` keyed by the factory function reference** (`sceneAppCache` in `SceneApp.js`), for the entire lifetime of the browser tab - `getClustersSceneApp()` (and every eagerly-constructed `$variables` inside it, including datasource-default variables reading `getDatasourceDefaults()`) runs **exactly once** per tab, at whichever moment `<App>` first renders. `AppConfig.tsx`'s `window.location.reload()` after saving does correctly force a fresh build for the admin's *own* tab, but any *other* already-open tab (or one whose initial Grafana bootstrap raced the config save) stays on stale defaults until its own hard reload - "config changes not being inherited" without any error.
- **New gotcha #34: `DataSourceVariable` (extends `MultiValueVariable`) silently falls back to `options[0]`** - the first datasource Grafana's own `getDataSourceSrv().getList({ pluginId })` returns for the *current viewing user* - whenever its current `value` doesn't exactly match one of the resolved options (`getStateUpdateGivenNewOptions` → `findOptionMatchingCurrent` fails → `getDefaultSingleState(options)` returns `options[0]`). This re-runs on every scene activation, not just once. Two realistic root causes for "wrong datasource is used": (a) the configured UID is stale/wrong (feeds directly from gotcha #33), or (b) **datasource permissions differ per user** - an admin who configured a specific RQLite instance may see it in their own `getList()`, while a different viewing user without query permission on that exact datasource would get a *different* first-available one instead, silently.
- Confirmed via `curl -X POST /api/ds/query` with two same-request targets (one Prometheus, one RQLite) that Grafana handles per-target errors gracefully - the RQLite target failed cleanly with its own `"plugin not registered"` error without affecting the Prometheus target's result. Useful smoke-test pattern for any future "does this Mixed-datasource wiring at least not crash the whole panel" question.
- **User then chose to skip debugging the above entirely and simplify instead**: dropped the whole OpenShift cluster-name-parsing + application/substage RQLite-indirection mechanism (`parseOpenshiftClusterCoordinates`, `buildNamespaceApplicationQuery`/`buildNamespaceSubstageQuery`, the `ovn_egressip_info` Prometheus query) in favor of one direct RQLite query keyed by the plain namespace name (`buildEgressIpQuery` in `namespaceOverviewQueries.ts`). Far fewer moving parts, much easier to reason about and debug going forward.
- **New gotcha #35: the g42-rqlite-datasource plugin's actual Panel JSON query shape, confirmed against a real user-built panel** (`spec.data.spec.queries[].spec.query.spec`): `rawSql` (the SQL text - confirms the "most likely" guess from the `v1.9.1` session's own note), `format: 'table'`, **`editorMode: 'code'`** (this is new information - if the backend branches on `editorMode` to decide whether to read `rawSql` directly vs. build a query from the builder-mode fields, omitting it could have been the *actual* remaining bug even with the right SQL field name), plus builder-mode scaffolding fields (`table`, `columns`, `whereClause`, `groupBy`, `orderBy`, `limit`, `offset`, `timeColumns: ['time']`) carried along even though unused in code mode. All baked into a new `buildEgressIpQueryTarget(namespace)` helper (also in `namespaceOverviewQueries.ts`) so both call sites stay in sync.
- **New gotcha #36: Mixed-datasource dispatch changes Prometheus's own field-naming disambiguation.** The Namespace Drilldown's info card combines a Thanos "info" query with the RQLite "egressip" query via Grafana's built-in Mixed datasource (`mixedDatasource()`, new in `queries/datasources.ts`, `uid: '-- Mixed --'` - confirmed as the literal string via `@grafana/runtime`'s own compiled source, not exported as a named constant from either `@grafana/data` or `@grafana/runtime`). Mixed dispatches each target to its own datasource's own separate backend sub-request - so Thanos now receives the "info" query **alone** again, and per gotcha #24's own mechanism, Prometheus stops disambiguating to `Value #info` and goes back to the bare `Value` name. This silently broke the "workloads:" row (showed "–") the moment the EgressIP query moved to Mixed - confirmed directly via `/api/v1/query` (same expression, sent alone vs. alongside a second query) before fixing the `fieldName` back to `'Value'`.
- The Workload Drilldown's own EgressIP row was never affected by gotcha #36 (its `SceneQueryRunner` only ever had the one `egressip` query, no Mixed needed there), so only `namespacesPage.tsx` needed the `fieldName` fix.

### Follow-up fixes found by the user live, after release
1. **"workloads:" info card row regression** (gotcha #36 above) - fixed by reverting the row's `fieldName` from `'Value #info'` to `'Value'`.
2. **Cluster Drilldown CPU/Memory tabs' "Namespaces" table had no Namespace column link at all** (pre-existing gap, not a regression - `NAMESPACES_URL` didn't even exist as a constant in `clustersApp.tsx` before this). Both `getClusterCpuScene`/`getClusterMemoryScene` gained a `cluster: string` parameter (previously only took `clusterRegex`) to build the link from, since this table has no `cluster` column of its own to read `${__data.fields.cluster}` from - the cluster name is inlined directly into the URL template instead, same as every single-cluster-scoped table elsewhere in this codebase.
3. **Namespace Drilldown's Overview/CPU/Memory "Workloads" tables had no Workload column link either** - same root cause as #2 (pre-existing gap). `getNamespaceCpuScene`/`getNamespaceMemoryScene` similarly gained `cluster`/`namespace` params (previously only regex forms) to build the link.

### Releases this session
- **`v1.9.2`**: the whole Workloads Drilldown page (all 7 tabs) + the EgressIP simplification (both New gotcha #35's confirmed query shape and the architecture change).
- **`v1.9.3`**: the two live-found fixes (`workloads:` regression, Cluster Drilldown CPU/Memory Namespace links).
- **`v1.9.4`**: the Namespace Drilldown Overview/CPU/Memory Workload column links.

All three released the same way as every prior release this repo has shipped (see "Release process" at the top) - feature commit(s) → `chore(release): vX.Y.Z` → tag → push, each triggering `.github/workflows/release.yml` successfully. **Package-lock hygiene**: reverted the unintended `@grafana/assistant` lockfile drift (see "Demo stack setup" above) before every one of this session's release commits, so only the deliberate `npm version` bump landed in each.

## Session 2026-08-19: Pod Drilldown + Node Drilldown pages (v1.10.0)

New worktree (`pod-drilldown-page-4cdaa8`), continuing from the prior session's `v1.9.4`. See the dedicated "### Pod Drilldown" and "### Node Drilldown" sections above in "What exists today" for the full architectural detail - this section covers how the session actually unfolded, gotchas, and the demo-stack/sandbox issues hit along the way.

### Demo stack: sandbox reset mid-session, full rebuild from scratch
Partway through the session, the sandbox environment itself was recycled (a fresh container/VM instance) - all previously-running podman containers were gone, `/tmp` was wiped (including the rootless-podman `XDG_RUNTIME_DIR` state and the extracted-Playwright-system-libs directory from gotcha #18), though `~/.config` and the git worktree itself persisted. First symptom was `podman ps` failing outright with `"cannot re-exec process to join the existing user namespace"` - **not** a stale-lock issue (gotcha #6's usual `alive.lck` fix didn't help); the actual cause was `XDG_RUNTIME_DIR` (`/tmp/podman/xdg`) no longer existing at all post-wipe. Fix: `mkdir -p "$XDG_RUNTIME_DIR/libpod/tmp" "$XDG_RUNTIME_DIR/containers"` again, same as the original one-time setup. Rebuilt the entire demo stack from scratch using the established recipe (base images were still cached, so this was fast) - `podman-compose -f docker-compose.demo.yaml up -d`, build+run Grafana via direct `podman run` with explicit volume paths (gotcha: the compose file's `extends:` bug), `/etc/hosts` patch in Grafana+Prometheus for the broken aardvark-dns.

**New gotcha: the Elasticsearch/RQLite *seed* containers (one-shot Python scripts, not long-running services) also need their own `/etc/hosts` patch**, not just Grafana/Prometheus - missed this the first time and both `demo-logs-000001`/RQLite tables silently never got seeded (the containers stayed "running" with zero log output, since Python buffers stdout when not attached to a TTY, masking the fact they were stuck retrying DNS resolution forever). Diagnosed by `podman exec`-ing into the seed container and running the same `urlopen()` call by hand, which surfaced the real `socket.gaierror`. Fixed by patching `/etc/hosts` in `debeka-k8s-app-demo_elasticsearch-seed_1`/`..._rqlite-seed_1` too, after which the existing retry loop picked it up within one 2-second cycle - no restart needed.

Playwright verification needed the system-libs extraction from gotcha #18 redone from scratch (same recipe, `apt-get download` + `dpkg-deb -x`, `LD_LIBRARY_PATH` prepended) since `/tmp` had been wiped - the extracted libs aren't preserved anywhere durable across a sandbox reset, so this is a "redo it every time the sandbox resets mid-session" cost, not a one-time setup.

### Building the Pod Drilldown page
Built incrementally across several turns, each its own explicit ask (not "build the whole thing at once"):
1. Page shell + Overview tab's two info cards (status/start time/restarts; cluster/node/namespace) + health banner. Reused `NamespaceHealthBanner` (`subject: 'Pod'`) as-is - no new banner needed here (unlike the Node Drilldown later, which did need one).
2. Follow-up: color the status/restart-count values, switch pod start time to an absolute `YYYY-MM-DD HH:mm:ss` format instead of relative. This is what surfaced the `InfoCard` `color` callback addition and the `format: 'table'` gotcha (see the "### Pod Drilldown" section above for both).
3. CPU/Memory/Network/Storage/Logs/Events tabs, all reusing the Workload Drilldown's own queries verbatim with `$pod` substituted for a literal name instead of a live variable - confirmed with the user up front ("the Workload Drilldown should already have a Pod filter in all queries") rather than assuming, since getting this wrong would have meant asking for corrected queries after the fact.
4. Follow-up: a "Containers" heading + combined CPU+Memory table between "Pod optimization" and "Logs / Events" on the Overview tab - mirrors the Workload Overview tab's own combined Pods table (which merges that page's CPU+Memory columns into one), one level deeper.
5. Follow-up: renamed the CPU/Memory *tabs'* own per-container tables from "Pods" to "Containers", swapped the identity column from pod-name to container-name, added IMAGE_SPEC, combined Requests+% into one cell. See the "Column-naming/scope corrections" note in the 2026-08-18 session above for the general pattern this follows (a request about "the table" refers to whichever one was just discussed, not every table on the page retroactively) - same principle applied again here, this ask was scoped to the CPU/Memory tabs' tables specifically, not the Overview tab's new "Containers" table from step 4.

### Building the Node Drilldown page
Also incremental: page shell + Overview tab's health banner + 3 info cards first (with the user specifying the health-status source and per-column field lists across two messages), then a follow-up fixing the info cards' row-height mismatch, then "Node optimization" + "Pods" table as a final follow-up with the exact PromQL given for both. Two real bugs surfaced by this last step, both described in the "### Node Drilldown" section above: the false-pressure health-banner bug (empty-result frame still counts as "some frame"), and the two demo-data/query issues in the Pods table (missing `node` label on two recording rules, an unfiltered `infoWaiting` query injecting phantom rows). All three were caught by actually running the page against the demo stack and screenshotting it, not just by code review - worth continuing to verify every new drilldown tab live rather than trusting the query translation alone, since PromQL syntax errors are not the only failure mode (empty-vs-missing results, unintended cross-node joins, and label-existence assumptions have all bitten a "looks right" query this session and the last).

### Releases this session
- **`v1.10.0`**: both the Pod Drilldown (all 7 tabs) and the Node Drilldown (Overview tab) in one release - two feature commits (`Add Pod Drilldown page...`, `Add Node Drilldown page...`), split from the single uncommitted end-of-session working tree by hand-splitting the two files touched by both features (`demo/kube-metrics/metrics`, `clusterOverviewCards.tsx`) into per-feature patches first (`git diff` → edit out the other feature's hunks → `git checkout --` the file → reapply each patch in its own commit), rather than one giant combined commit - confirmed each reconstructed file was byte-identical (`md5sum`) to the real pre-split working tree state before trusting the split.

## Session 2026-08-21: two bugfixes reported by the user (v1.11.2)

New worktree, starting point `v1.11.1`. User-reported (in German): (1) the Kubernetes home page's table has no links to Cluster/Namespace/Pod Drilldowns, (2) the Clusters list's "Alerts" column disagrees with the Cluster Drilldown - Cluster A shows 5 there, 20 after clicking into its Drilldown, and 20 matches the Alerts page too, so the bug is on the Clusters list specifically. Also asked about adding Grafana-Enterprise-Insights-style usage analytics (who/when/how-often uses the app) - answered via `AskUserQuestion` that this plugin is frontend-only (`"type": "app"`, no backend/executable in `plugin.json`) so real per-user usage tracking would need a new backend component; **user explicitly declined** ("Dann lassen wir es. Es soll eine reine Frontend Applikation bleiben") - not pursued, don't resurface unless asked again.

**Bug 1 root cause**: "the table" = the shared "Issue details" table at the bottom of the Overview tab (`getKubernetesOverviewScene` in `kubernetesOverviewScene.tsx`) - the one table behind every Availability/Stability/Infrastructure mini panel's "View detail" button (see the `DetailViewSelection`/`ViewDetailLink` mechanism documented earlier in this file). It never had `.setOverrides(...)` at all, unlike every other list/detail table in the app. Fixed by adding `matchFieldsWithName('cluster'|'namespace'|'node'|'pod')` overrides with the same native-table-link idiom (`__data.fields.X`/`__value.text`) used everywhere else - safe against this table's real complication, that its 12 swapped-in issue queries (`kubernetesIssueQueries` in `kubernetesOverviewQueries.ts`) carry different label sets (cluster+node only for node-level issues; cluster+namespace+pod, +workload/workload_type when the join succeeds, for pod-level issues; cluster+namespace+deployment for the one deployment-level issue) - `matchFieldsWithName` only touches whichever of these fields is actually present in the currently-selected query's result, so one override set safely covers all 12 without per-query branching. The pod link needs `workload`/`workload_type` (from each query's own `group_left(workload, workload_type)` join against `namespace_workload_pod:kube_pod_owner:relabel`) to build a full `${WORKLOADS_URL}/.../pods/${pod}` URL - the few issue queries with an explicit `unless` fallback branch for genuinely-unowned pods (missing from that join) would produce a broken pod link in that edge case, not fixed here (same edge case as gotcha #29, out of scope for a "links are missing" report).

**Bug 2 root cause**: `clusterTableQueries.alerts` (`clusterQueries.ts`) was `count by (cluster) (ALERTS{alertname=~"(Kube.*|CPUThrottlingHigh)", ...} or GRAFANA_ALERTS{alertname=~"(Kube.*|CPUThrottlingHigh)", ...})` - restricted to a specific alertname allowlist plus an OR'd-in `GRAFANA_ALERTS` branch, apparently translated literally from Grafana Play at some earlier session. Both the Cluster Drilldown's own alerts badge (`clustersApp.tsx`'s `alertsBadgeRunner`) and the Alerts page's own queries (`alertsPage.ts`) count *every* firing `ALERTS` series, only excluding `alertname!~"ArgoCDSyncAlert"` - no allowlist, no `GRAFANA_ALERTS`. Fixed by simplifying the Clusters list query to match exactly - same fix/reasoning as the Nodes list's own "Alerts" column got in `v1.11.1` (`nodeQueries.ts`, already using the plain-`ALERTS`-count pattern, referenced directly as the precedent while writing this fix). Confirmed via live `curl` against the demo Prometheus (`/api/v1/query`, old query vs. new query vs. the Alerts page's own filter set) that the fix produces numbers matching the Drilldown/Alerts page - the demo dataset's 4 firing alerts all happen to already satisfy the old allowlist, so this specific discrepancy isn't visually reproducible in the demo (would need a firing alert with a non-`Kube*`/non-`CPUThrottlingHigh` name to demonstrate live), but the code-level comparison and the query-level PromQL comparison both confirm the root cause and the fix.

**Demo stack setup this session**: fresh sandbox reset (same rootless-podman recipe as prior sessions - `storage.conf`/`containers.conf` already present from a previous session, just needed `XDG_RUNTIME_DIR` + its two `mkdir -p` dirs recreated). This worktree's own `node_modules` didn't exist at all (not even the shared-symlink fallback worked cleanly - `npx tsc`/`npx eslint` silently fell back to `/opt/debeka-k8s-app/node_modules`, which is missing `@grafana/assistant`, breaking `npx webpack`) - did a full `npm ci` in-worktree (~1250 packages, ~1 min) rather than chasing the shared directory's drift, per gotcha #7. Grafana image build (`podman-compose -f docker-compose.yaml build`, `grafana-enterprise:13.1.0` base) took several minutes for the layer pull; **this sandbox's wall clock only advances while a tool call is actively running** - `ScheduleWakeup`'s `delaySeconds` and a backgrounded `sleep`-based Bash watcher both returned almost immediately regardless of the requested delay, with the underlying `podman build` process's own `ELAPSED` barely ticking up between checks - only `Monitor` (or repeated foreground polling) reliably waited for the real background process to finish. **New gotcha #37: the `prom/prometheus` container does not run as root** (unlike this repo's own Grafana image, which the compose file explicitly runs `--user root`) - patching its `/etc/hosts` for the aardvark-dns workaround needs `podman exec -u root prometheus sh -c "..."`, plain `podman exec prometheus ...` fails with "Permission denied". This was already implied by the existing "patch /etc/hosts in Grafana and Prometheus" line in the Demo stack recipe below, but the *why plain exec fails* wasn't previously spelled out - worth remembering directly next time instead of re-discovering it.

**Verification**: Playwright (same extracted-system-libs recipe as gotcha #18, re-extracted fresh into this session's scratchpad - not preserved from a prior session). Screenshotted the Clusters list (3/1 alerts) and the Cluster Drilldown badge (3 firing alerts) side by side, confirming they now agree. Switched the Kubernetes home page's "Detail view" dropdown to "Pods not ready" (a query with cluster+namespace+pod+workload+workload_type all present) and confirmed all three link hrefs resolve to real, distinct URLs (`/clusters/demo-cluster-aws`, `/namespaces/demo-cluster-aws/default`, `/workloads/demo-cluster-aws/default/pod/debug-shell-aws/pods/debug-shell-aws`), then navigated the pod link through to a fully-rendered Pod Drilldown page. `tsc --noEmit`/`eslint` clean on both changed files.

### Release
- **`v1.11.2`**: both fixes in one release, following the established process (feature commit with its own CHANGELOG bullets → `chore(release): v1.11.2` → tag → push).

## Session 2026-08-21 (second): Alerts/Workload link fixes + app-wide audit (v1.11.3)

New worktree (`app-links-improvements-7af61f`), starting point `v1.11.2`. Two user-reported fixes (in German), then an explicit "check the whole app for anything else, and propose improvements/cool features".

**Note the summary was already stale on arrival again**: it claimed the Node Drilldown had only its Overview tab built, but `v1.11.0` had shipped its CPU/Memory/Network/Storage tabs (Logs/Events stay placeholders by explicit user decision - no reference content and no node-identifying field in the demo Elasticsearch data). `CHANGELOG.md` was right, this file wasn't. Same warning as the top-of-file note: check `CHANGELOG.md`/`git log` before trusting "what exists today".

### The two reported fixes
1. **Alerts page table had no Cluster/Namespace/Pod links.** The Pod link is the hard part: the Pod Drilldown's route is nested under its owning workload, and `ALERTS` has no `workload` label. The query now attributes alerts in **three mutually-exclusive branches** - `or` only drops a right-hand series whose label set matches the left *exactly*, so overlapping branches would duplicate rows:
   - `ALERTS{..., pod!=""} * on (cluster,namespace,pod) group_left(workload, workload_type) namespace_workload_pod:kube_pod_owner:relabel`
   - the same selector `unless on (cluster,namespace,pod)` that record, wrapped in the `label_replace(..., "workload", "$1", "pod", "(.+)")` + `label_replace(..., "workload_type", "pod", "", "")` bare-pod fallback (without this the demo's own alerts, which all sit on the ownership-record-less `app-0` pod, would have produced `/workloads/c/ns///pods/app-0`)
   - `ALERTS{..., pod=""}` untouched, for node-/cluster-level alerts
   Verified live against the demo Prometheus that all three together return the same 4 rows as the original selector, no duplicates and no `many-to-many`. **This adds WORKLOAD and TYPE columns** - TYPE only exists because the link needs `${__data.fields.workload_type}` and there is no way to keep a field in the frame but out of the table (see gotcha #19; `hideHeader`/`hideFrom` are both silently dropped by the Table panel's field-config registry), so it is pinned to `width: 90`.
2. **Workload Drilldown CPU/Memory tabs' "Pods" tables had no POD link.** `getWorkloadCpuScene`/`getWorkloadMemoryScene` gained a `podBaseUrl` param built from the route params at the call site - same precedent as `getClusterCpuScene`/`getNamespaceCpuScene` gaining `cluster`/`namespace` params in the 2026-08-18 session, and necessary because these tables `excludeByName` cluster/namespace/workload from the frame.

### Found by the audit (all fixed)
- **The Efficiency tab's five tables had no links at all** - same class of bug as the two above and as `v1.11.2`'s own Issue-details fix. This was the third-and-fourth copy of the same override block, so it was extracted into **`applyEntityDrilldownLinks` (`src/scenes/drilldownLinks.ts`)**, now used by the Alerts table, the Issue-details table and the Efficiency tables. **Reach for this helper for any new table** rather than writing the overrides again; it also covers a bare `deployment` column (the "Zero replica deployments" query returns `cluster`/`deployment`/`namespace` only, no workload_type, so the type segment is a literal there).
- **"Scheduling: Containers with CPU/Memory requests set" showed 100% in red** on all six Namespace/Workload/Pod CPU+Memory tabs: it reused `usageThresholds`, which is a *utilisation* scale (>90% = about to hit the ceiling), for a *coverage* ratio where 100% is the goal. New mirrored `coverageThresholds` in `tableCells.tsx`. **If a new stat panel is a "% of things correctly configured", use coverageThresholds; if it is "% of capacity consumed", use usageThresholds.**
- **`App.test.tsx` had been failing since v1.7.0** - it destructured the Clusters page out of `pages[0]`, and the Kubernetes home page was inserted ahead of it. Now looks up by title. (Distinct from the *e2e* suite, which the top of this file already documents as failing pre-existing.)
- **17 dead exports in `src/queries/prometheus.ts`** (`countClusters`, `firingAlerts`, `clusterInventory`, `clusterCpuUsage`, `podPhases`, ...) - early-iteration builders superseded by the per-page `queries/*Queries.ts` files, zero consumers. Removed; only `EntityScope`/`scopedMatchers` survive (used by `resourceSimulator.ts`).
- **`SectionHeading` was declared identically in six files**, and the Kubernetes home page's two scenes imported it from `clustersApp.tsx` - the two import cycles graphify flagged. Now `src/scenes/sectionHeading.tsx`. The long "use a Button + window.location, not an <a href>, or the cluster variable collides" comment that sat above it in `clustersApp.tsx` actually describes `ClusterOverviewLinks` and was moved onto that function; several other files' comments cite it by name.

### Three features built (all explicitly chosen by the user from a proposal list)
- **"Copy as kubectl"** (`src/scenes/kubectlCell.tsx`): icon-only **first** column (`addKubectlField` transform + `applyKubectlColumn` overrides) on the Namespaces/Workloads/Nodes list tables, the Workload and Node Drilldowns' Pods tables and the Alerts table, opening a `Dropdown`+`Menu` of ready-to-paste commands with the command itself as each item's `description`. Command set differs per entity kind (pod: describe/logs/yaml/events/exec; workload: describe/logs/yaml/list-pods/events; node: describe/pods-on-node/cordon/drain; namespace: describe/get-all/quotas/events). `fixed` scope values come from route params where the table has no such column. Two things learned: (a) **the column had to move to index 0** - at the end it fell off the right edge of every already-wide list table; (b) **`hideHeader: true` is silently dropped by the Table panel** (same registry mechanism as gotcha #19), so the header is blanked with `overrideDisplayName(' ')` instead. Also note `IconButton` derives its own `aria-label` from `tooltip` and ignores an explicit one.
- **Rollout/restart annotation markers** (`src/scenes/changeAnnotations.ts`): `dataLayers.AnnotationsDataLayer` pairs on the Workload and Pod Drilldown **pages** - `sceneGraph.getDataLayers` walks up from each panel and collects any ancestor whose `$data` is a data layer, and a tab's `EmbeddedScene`'s parent is the `SceneAppPage`, so **one layer set on the page covers all 7 tabs** with one `SceneDataLayerControls` in the page controls. Expressions follow Grafana's own documented `changes(...) > 0` shape (fetched from grafana.com, not memory - the modern docs use field `mappings`, not the legacy `titleFormat`/`useValueForTime`). **New gotcha #38: `SceneDataLayerControls` renders ONE `ControlsLabel` from the enclosing `SceneDataLayerSet`'s own `name` (default literally `"Data layers"`) plus one *unlabelled* `InlineSwitch` per layer** - `AnnotationsDataLayerRenderer` has no label of its own - so with 2+ layers the toggles are indistinguishable unless the set's `name` spells them out; here it's built as `layers.map(l => l.state.name).join(' / ')`. **These markers can never fire against the local demo stack** (static metrics file → `changes()` is always 0); the queries were validated for syntax/shape against the live Prometheus instead, and `kube_*_metadata_generation` was added to the demo data so they resolve against a real metric name.
- **App-wide "Investigate"** (`src/scenes/investigateEntityButton.tsx`): the same `OpenAssistantButton` the Alerts table's Action column uses, now next to the entity badge in every Drilldown page title (Cluster/Node/Namespace/Workload/Pod). It renders `null` when no Assistant plugin is installed, which is why it is invisible in the local demo stack - not a bug. The Alerts table's own prompt/context gained `node`/`workload`/`workload_type`, and its **Action column moved to index 0** (it used to sit permanently off-screen behind horizontal scroll - the app's flagship integration was the least reachable thing on the page), alongside a new linked **NODE column** (the page had a Node filter all along but never showed the node).

### Verification approach worth reusing
The demo stack was already running **from a different worktree** (`kubernetes-links-alert-count-bugs-e8d2b5`, per `podman inspect`'s mount paths) with another session presumably using it. Rather than recreating the shared `debeka-k8s-app` container and breaking that session, ran a **second Grafana container** (`k8sapp-verify`, port 3001) on the same `debeka-k8s-app-demo` network, bind-mounting this worktree's own `dist/`/`provisioning/`, reusing the already-built image, with the usual `/etc/hosts` patch. Cheap, fully isolated, torn down afterwards. **Do this instead of repointing the shared stack whenever `podman inspect` shows a different worktree.** (Consequence: `demo/kube-metrics/metrics` edits made here are *not* live - `kube-metrics-demo` still serves the other worktree's copy - so the new `kube_*_metadata_generation` rows are unverified against a running Prometheus.)

Playwright needed the gotcha #18 system-libs dance again, but this time only `libnspr4`/`libnss3`/`libasound2t64` were missing (`~/.cache/ms-playwright` itself was already populated, and the project's own `playwright` npm package resolved fine once `npm ci` had run in-worktree).

### Release
- **`v1.11.3`**: six commits (links fix / thresholds fix / test fix / dead-code removal / the three features + SectionHeading extraction combined / CHANGELOG) → `chore(release): v1.11.3` → tag → push. The three features landed as **one** commit on purpose: they interleave in the same five page files and separating them would have needed hunk-level surgery on single lines - the same call documented under "Committing after heavily interleaved edits" at the top of this file, and the commit message says so explicitly.

### Proposed but not built (user picked a subset)
Column-header casing on the Issue-details/Efficiency tables (raw lowercase `cluster`/`pod`/`Value` vs. the app's ALL-CAPS convention elsewhere) was offered and **not** chosen. Also raised and not pursued: favourites/pinned namespaces on the home page, sparkline trend columns in list tables.

### Follow-up same session: oc instead of kubectl, one Action column (v1.11.4)

User feedback on the v1.11.3 row action, three parts:
1. **`oc`, not `kubectl`** - the org runs OpenShift. `src/scenes/kubectlCell.tsx` -> `ocCell.tsx`, exports renamed (`ocCommandsFor`/`OcActionButton`/`ocCell`/`applyOcActionColumn`/`OcScope`/`ocScopeForRow`). Not a pure rename of the binary: `oc rsh <pod>` replaces `kubectl exec -it ... -- sh`, `oc adm cordon`/`oc adm drain` replace the top-level kubectl verbs, `oc get events --field-selector involvedObject.name=X --sort-by=.lastTimestamp` replaces `kubectl events --for` (which older `oc` builds lack), and a namespace gets `oc describe project`.
2. **One Action column.** The Alerts page had ended up with two adjacent action columns. `InvestigateActionCell` now renders the Assistant button *and* `OcActionButton` side by side in one flex row, and `addActionField` lives in `ocCell.tsx` and is re-exported from `investigateActionCell.tsx` so both paths add the same single `action` field. The other five tables' formerly-nameless icon column is `overrideDisplayName('Action')`. Alerts' Action column width 120 -> 190 for the two buttons; the standalone one is 84.
3. **A Button, not an IconButton** - `Button` with `variant="secondary" fill="solid" size="sm"`, `icon="clipboard-alt"`, label `oc`, matching `OpenAssistantButton`'s own shape (read from `@grafana/assistant`'s compiled `OpenAssistantButtonView`). **Deliberately not** copying that button's purple/orange gradient-border `className` - that is the Assistant's branding, and this button isn't the Assistant.

**Playwright selector gotcha while verifying**: `button:has-text("oc")` is a *substring* match and picks up unrelated buttons on a busy page (it reported 3 matches on the Workload Drilldown and clicked the wrong one). Match on the button's own `title` attribute (`button[title="Copy an oc command for this row"]`) instead.

## Deploying to localhost:3000 + the Assistant plugin (same session)

The user asked for the build on their own `localhost:3000` plus the custom Assistant app installed.

**Taking over the shared container**: `:3000`'s `debeka-k8s-app` was bind-mounted to a *different* worktree. Recreated it against this one (plus `kube-metrics-demo`, so the new `kube_*_metadata_generation` demo rows actually land - Prometheus confirmed 3 series). Told the user this takes the container away from whatever session had it.

**New gotcha #39: the demo containers have no working DNS at all**, not just no container-name resolution. `/etc/resolv.conf` lists only `10.89.0.1` (the aardvark gateway that never starts), so *external* names fail too - visible in the Grafana log as `dial tcp: lookup grafana.com on 10.89.0.1:53: connection refused` for every plugin the enterprise image tries to preinstall. **`podman run --dns 1.1.1.1` does NOT help** - podman's own network DNS overrides it when the network has `dns_enabled: true`. Fix: overwrite `/etc/resolv.conf` inside the container the same way `/etc/hosts` is patched (`cat` a new file over it - `sed -i` fails, it's a bind-mounted special file). **This resets on `podman restart`, exactly like the `/etc/hosts` patch.**

**Installing the custom Assistant**: `elohmeier/grafana-pi-app` (the colleague's repo; `Lprod`'s fork is behind and the three fixes it carried are merged upstream). Its release publishes **two** artifacts from two workflow jobs - the signed `g42-pi-app-<v>.zip` under the repo's own plugin id, and a `package-plugin-variant` job that rebuilds with `PLUGIN_VARIANT_ID=grafana-assistant-app`. **The variant is the one to install**: `@grafana/assistant`'s `OpenAssistantButton` looks for the `grafana-assistant-app` plugin id, so the `g42-pi-app`-id build would register fine and still leave every Investigate button rendering `null`. Only the variant asset was actually attached to the v3.0.0 release.
- Installed with `podman exec -u root debeka-k8s-app grafana cli --pluginsDir /var/lib/grafana/plugins --pluginUrl <release-url> plugins install grafana-assistant-app` (after the DNS fix above), then `podman restart`, then re-patch `/etc/hosts` *and* `/etc/resolv.conf`, then `POST /api/plugins/grafana-assistant-app/settings {"enabled":true,"pinned":true}` - an app plugin is registered but **not enabled** by installation alone.
- Container env needed `GF_PLUGINS_ALLOW_LOADING_UNSIGNED_PLUGINS` to include `grafana-assistant-app` (the variant build is unsigned - no MANIFEST.txt) and `GF_FEATURE_TOGGLES_ENABLE=extensionSidebar`.
- **Grafana's own background installer also wants `grafana-assistant-app` from grafana.com** (it's in the enterprise image's preinstall list). It skips already-installed plugins, so the custom build survived once DNS started working - but verify the installed `info.author` (`Enno Richter`) rather than assuming, since the official Grafana Assistant would occupy the exact same id and directory.
- **Still not usable end to end**: v3.0.0 is a breaking change that removed the top-level `defaultModel`/`openAIProtocol`/`thinkingLevel`/`thinkingFormat` settings in favour of a **models list**. Nothing is configured, so the sidebar opens but can't answer. The user has to point it at their internal LLM endpoint.

**Guardrail note for future sessions**: several steps here were blocked by the environment's classifier - `gh release download`, `unzip`ing the downloaded artifact, and `GF_INSTALL_PLUGINS=<url>` on `podman run`. A plain `curl` of the asset (with SHA1 verified against the published checksum) and letting **Grafana's own `grafana cli ... --pluginUrl`** do the download+extract went through, and is the more idiomatic route anyway. Don't reach for a hand-rolled unpack.

### Follow-up: the oc button got the gradient border after all (v1.11.5)

v1.11.4 deliberately left the `oc` button without the Assistant's purple/orange gradient border, on the reasoning that it is the Assistant's branding and this button isn't the Assistant. **The user overruled that** ("mach den oc Button fancy, mit dem schimmernden violett/orangen Rand wie bei Investigate") - so it now carries it. Worth remembering as a taste call: in this app the two row actions are meant to read as a *pair*, and matching the visual treatment wins over keeping the Assistant's styling exclusive.

Implementation is a port of `@grafana/assistant`'s own compiled `getStyles` (read out of `node_modules/@grafana/assistant/dist/esm/components/button/OpenAssistantButton.js`), not an approximation - `getOcButtonStyles` in `ocCell.tsx`. The mechanism, in case it needs touching again: a `::before` layer paints `linear-gradient(90deg, rgb(168,85,247), rgb(249,115,22))` across the full border radius at `zIndex: -2`, and a `::after` layer inset by the 1px border width paints the button's own background back over the middle at `zIndex: -1` - only the 1px ring is left visible. The `::after` background has to be layered over `theme.colors.background.canvas` (`linear-gradient(bg, bg), canvas`) because `theme.colors.secondary.main` is semi-transparent and would otherwise let the gradient bleed through the face of the button. `isolation: 'isolate'` on the button keeps both negative-z-index layers behind the label but above whatever the button sits on. The border is a *static* gradient, not animated, despite "schimmernd" in the request - that matches Investigate exactly, which is what was asked for.

**Verification tip**: to eyeball a small control like this, screenshot with Playwright's `deviceScaleFactor: 3` and a `clip` box derived from the element's own `boundingBox()` - a full-page shot at scale 1 is far too coarse to tell a 1px gradient ring from a plain border.
